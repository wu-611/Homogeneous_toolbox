# 齐次控制（Homogeneous Control）基础原理与应用范围

> 基于 Polyakov (2020) _Generalized Homogeneity in Systems and Control_ 及相关论文整理。

---

## 1. 什么是齐次控制

齐次控制是一种非线性控制器设计方法，核心思想是利用**膨胀对称性（dilation symmetry）**将已有的线性控制器升级为具有更好收敛性质的非线性齐次控制器。

### 1.1 膨胀群（Dilation Group）

定义线性膨胀算子 `d(s) : ℝⁿ → ℝⁿ`：

```
d(s) = expm(s · Gd),   s ∈ ℝ
```

其中 `Gd ∈ ℝⁿˣⁿ` 是 anti-Hurwitz 矩阵（即 `-Gd` 为 Hurwitz），称为**膨胀生成元**。

膨胀群的关键性质：

```
d(s₁) · d(s₂) = d(s₁ + s₂)    （群同态）
d(0) = I                       （单位元）
lim_{s→±∞} ||d(s)x|| = ±∞     （极限性质）
```

膨胀赋予向量空间一个**新的尺度概念**——它不是均匀缩放所有坐标，而是按照 Gd 的特征结构对各方向进行**各向异性缩放**。

### 1.2 典范齐次范数（Canonical Homogeneous Norm）

给定正定矩阵 P（满足 `P·Gd + Gdᵀ·P ≻ 0` 以保证膨胀单调），定义齐次范数 `||x||_d`：

```
||x||_d = exp(c),   其中 c 满足: yᵀ·P·y = 1,  y = expm(-c·Gd)·x
```

等价地：

```
xᵀ · dᵀ(-ln||x||_d) · P · d(-ln||x||_d) · x = 1
```

齐次范数的核心性质：

```
||d(s)x||_d = eˢ · ||x||_d    （齐次性）
||x||_d = 0  ⇔  x = 0         （正定性）
||λx||_d ≠ |λ|·||x||_d         （非绝对齐次，除非 Gd = I）
```

计算上通过**二分法**求解：在 `a`（下界）和 `b`（上界）之间搜索 `c`，迭代至精度 `1e-6`。

### 1.3 齐次向量场与齐次度

向量场 `f : ℝⁿ → ℝⁿ` 称为 **d-齐次的**，齐次度为 μ ∈ ℝ，如果：

```
f(d(s)x) = e^{μs} · d(s) · f(x),   ∀x ∈ ℝⁿ, ∀s ∈ ℝ
```

**定理 1（Zubov-Rosier）**：如果系统 `ẋ = f(x)` 有 d-齐次度为 μ 的连续向量场且全局渐近稳定，则：

| μ | 收敛类型 |
|---|---------|
| μ < 0 | **有限时间稳定**：存在 T(x₀) 使 x(t) = 0, ∀t ≥ T |
| μ = 0 | **指数稳定**（退化为线性系统） |
| μ > 0 | **近似固定时间稳定**：存在与初值无关的 T(r) 使 ||x(t)|| < r, ∀t ≥ T |

### 1.4 齐次控制律：通用升级公式

给定线性控制器 `u = Kx`，通过 `lpc2hpc(A, B, K)` 计算出齐次化增益 `K₀` 和膨胀生成元 `G₀` 后，齐次控制律为：

```
u = K₀·x + ||x||_d^(1+μ) · (K - K₀) · d(-ln||x||_d) · x
```

其中：
- **K₀·x**：线性分量，确保在平衡点附近的局部行为
- **||x||_d^(1+μ)·(K-K₀)·d(-ln||x||_d)·x**：非线性分量，根据误差大小自适应缩放
- **μ**：齐次度，控制收敛速率
- **d(-ln||x||_d)·x**：将状态投影到单位球面上（`||π(x)|| = 1`）

物理直觉：
- 当 `||x||_d > 1`（远离平衡点）且 `μ < 0` 时，`||x||_d^(1+μ) < 1` → **衰减**控制，避免 overshoot
- 当 `||x||_d < 1`（接近平衡点）且 `μ < 0` 时，`||x||_d^(1+μ) > 1` → **放大**控制，加速收敛
- 当 `μ = -1` 时，`||x||_d⁰ = 1` → 无幅值改变，仅重定向

---

## 2. 升级流程：从线性控制器到齐次控制器

### 2.1 lpc2hpc(A, B, K) 算法

```
输入:  A (n×n), B (n×m), K (m×n)  — A+BK 必须为 Hurwitz
输出:  K₀, G₀, P, μ_min, μ_max

步骤1: [T, nt] = block_con(A, B)
       将 {A, B} 变换为块可控典范型

步骤2: 计算 K₀ = -B₀'·(B₀B₀')⁻¹·A_last_row
       使 A₀ = A+BK₀ 在最后一块行幂零

步骤3: 计算膨胀生成元
       vG₀ = [k-1 重复 n₁次, k-2 重复 n₂次, ..., 0 重复 nₖ次]
       G₀ = -T⁻¹·diag(vG₀)·T
       Gd = I + μ·G₀

步骤4: 求解 Lyapunov 方程
       (A+BK)'P + P(A+BK) = -2I
       得到形状矩阵 P

步骤5: 计算 μ 的容许范围 [μ_min, μ_max]
       通过 sqrt(P)·G₀·sqrt(P)⁻¹ + sqrt(P)⁻¹·G₀'·sqrt(P) 的特征值
```

### 2.2 为什么必须使用 "积分链"（块可控型）

**关键**：步骤 2 要求 `A₀ = A + BK₀` 在最后一块行上**幂零**。这只有当 A 是**块可控典范型**时才成立。

块可控典范型的形式：

```
A = [0    I    0   ...  0 ]
    [0    0    I   ...  0 ]
    [...                 I ]
    [*    *    *   ...  * ]
B = [0; 0; ...; I]
```

这本质上就是**积分链**（integrator chain）：`ẋ₁ = x₂, ẋ₂ = x₃, ..., ẋₖ = u`。每一个状态是上一个状态的导数。

只有在这种结构中：
- 膨胀生成元 `G₀` 才能具有 `[k-1, k-2, ..., 0]` 的规范形式
- Lyapunov 方程的解 P 才能保证 `P·Gd + Gdᵀ·P ≻ 0`（膨胀单调性）
- 齐次闭环系统才能保持 d-齐次性

**这就是齐次控制"只能用于积分链模型"的根本原因**——不是理论限制，而是 `lpc2hpc` 算法对系统结构的要求。

---

## 3. 应用范围：从 ℝⁿ 到 SO(3)

### 3.1 可以直接应用的场景

任何可以写为**积分链**（或通过反馈线性化变为积分链）的系统：

| 系统 | 阶数 | 状态 |
|------|------|------|
| 单积分器 | 1 | [x] |
| 双积分器 | 2 | [x; ẋ] |
| N 阶积分器 | n | [x; ẋ; ...; x^{(n-1)}] |
| 旋转倒立摆 (QUBE-Servo) | 4 | 块可控型 |
| 多智能体编队 (双积分器) | 4 | [x, y, vx, vy] |
| 四旋翼位置回路 | 4-8 | 级联积分链 |

所有这些系统都可以直接使用 `lpc2hpc` / `lpic2hpic`。

### 3.2 不能直接应用的场景

膨胀算子 `d(s) = expm(s·Gd)` 和齐次范数 `||x||_d` 的定义**依赖**于：
1. 状态空间是**向量空间**（ℝⁿ）
2. `expm` 是**矩阵指数**（定义在 ℝⁿˣⁿ 上）
3. `xᵀPx` 是**欧氏内积**（定义在 ℝⁿ 上）

因此不能直接应用于：
- **SO(3)（旋转矩阵）**：不是向量空间，是紧李群
- **SE(3)（刚体位移）**：不是向量空间，是李群
- **S³（单位四元数）**：不是向量空间，是流形
- 任何定义在**弯曲流形**上的系统

### 3.3 为什么 SO(3) 不能直接定义膨胀

如果尝试对 SO(3) 上的旋转矩阵 R 定义膨胀 `d(s) : SO(3) → SO(3)`，需要：

```
d(s₁) · d(s₂) = d(s₁ + s₂)    （群同态）
d(s) ∈ Aut(SO(3))             （保持群结构）
```

但 SO(3) 是**紧李群**——任何连续自同构群都是共轭作用，不能产生真正的"缩放"效果。这是微分几何的基本事实：紧流形不允许非平凡的膨胀。

---

## 4. 扩展路径 1：在 Lie 代数 so(3) 上设计

> 参考：Zhou, Polyakov & Zheng (2023). _Generalized Homogeneous Rigid-Body Attitude Control_.

### 4.1 核心思路

**不直接在 SO(3) 上定义膨胀，而是在其 Lie 代数 so(3) ≅ ℝ³ 上定义。**

```
SO(3) ──Log──→ so(3) ≅ ℝ³  (向量空间 ✓)
                 ↓
             齐次控制器 u_hom  (在 ℝ⁶ = ℝ³×ℝ³ 上)
                 ↓
实际力矩 M ←── Exp 映射回 SO(3)
```

### 4.2 姿态误差的指数坐标

姿态误差 `R_e = R R_dᵀ` 表示为指数坐标 `θ_e ∈ ℝ³`：

```
R_e = Exp(θ_e) = exp(θ_e^)
```

`θ_e ∈ B³[π]`（半径为 π 的闭球）。Log 映射在 B³(π) 上是双射，在边界 `|θ_e| = π` 上出现歧义（`±πy` 代表同一个旋转）。

### 4.3 全局动力学：脉冲系统

由于边界歧义，必须引入**跳变机制**（脉冲系统）：

```
当 |θ_e| = π 且 d/dt|θ_e| > 0 时:
    θ_e → -θ_e     (Π = [-I₃ 0; 0 I₃])
    ω_e → ω_e
```

系统在 `C = B³(π) × ℝ³` 上连续演化，在边界 `D = {ξ: |θ_e|=π, θ_eᵀω_e>0}` 上跳变。

### 4.4 齐次控制器定义

在 `ξ = [θ_e; ω_e] ∈ ℝ⁶` 上定义齐次控制器：

```
u_hom = ||ξ||_d^(1+μ) · K · d(-ln||ξ||_d) · ξ
K = [-K₁  -k₂I₃],   K₁ ≻ 0, k₂ > 0
```

膨胀生成元：

```
Gd = [(1-μ)I₃    0   ]
     [   0       I₃  ]
```

注意：这是**加权膨胀**（weighted dilation），与积分链的 G₀ 结构不同。姿态误差和角速度有不同的膨胀权重。

形状矩阵 P：

```
P = [  I₃     εI₃  ]
    [ εI₃   K₁⁻¹  ]
```

ε 的选择需满足：`0 < ε < min{ε_μ, ε̃}`，保证 `P ≻ 0`, `PGd + GdᵀP ≻ 0`, 以及 Lyapunov 导数负定。

### 4.5 实际力矩映射

```
M = J [R_dᵀ · u_hom - ω_d^× · ω + ω̇_d] + ω × Jω
```

其中 `J_r(θ_e)` 是右 Jacobian，满足 `ė_e = J_r⁻¹(θ_e) · ω_e`，在 `|θ_e| ≤ π` 上有界。

### 4.6 跳变相容性条件

冲击系统保持齐次性质需要两个条件（Theorem 2）：

**条件 1**：`G_d Π = Π G_d` —— 膨胀与跳变交换
```
[(1-μ)I₃    0  ] [-I₃  0]   [-I₃  0] [(1-μ)I₃    0  ]
[   0       I₃ ] [ 0   I₃] = [ 0   I₃] [   0       I₃ ]
✓ 成立
```

**条件 2**：`yᵀΠᵀPΠy ≤ 1, ∀y ∈ π_d(D)` —— 跳变时 Lyapunov 函数不增加

论文证明（Remark 1）：当 `θ_eᵀω_e > 0` 时（跳变条件），翻转后 `θ_e → -θ_e` 使得 P 内积不增。✓ 成立。

### 4.7 收敛性保证

| μ | 行为 |
|---|------|
| μ < 0 | 全局有限时间精确收敛到零 |
| μ = 0 | 退化为经典指数坐标线性 PD 控制器 |
| μ > 0 | 全局近固定时间收敛到零的邻域 |

**固定时间控制**（Corollary 1）：切换 `μ₁ > 0`（远离原点时）和 `μ₂ < 0`（靠近原点时）：

```
ξᵀPξ ≥ 1 时用 μ₁  →  固定时间内进入单位球
ξᵀPξ ≤ 1 时用 μ₂  →  有限时间内精确收敛到零
```

总收敛时间上界：`T ≤ 1/(ρ̃₁μ₁) + 1/(-ρ̃₂μ₂)`，**与初值无关**。

---

## 5. 扩展路径 2：在四元数上设计

### 5.1 四元数与 so(3) 的等价性

四元数和 so(3) 指数坐标是**同一事物的两种参数化**：

| | so(3) | 四元数 |
|---|---|---|
| 状态空间 | θ ∈ B³[π] | q ∈ S³ |
| 物理含义 | θ = φ·n | q = [cos(φ/2); n·sin(φ/2)] |
| 歧义处理 | `\|θ\|=π` 时 `θ→-θ` | `q₀<0` 时 `q→-q` |
| Lie 代数 | ℝ³ | 纯虚四元数 ≅ ℝ³ |

两者的 Lie 代数都是 ℝ³ → **膨胀定义在两个框架中等价**。

### 5.2 四元数框架的适配

误差定义：

```
q_e = q_d* ⊗ q = [q₀; q_v]
θ_e = 2 · arccos(|q₀|) · (q_v / |q_v|)    当 q_v ≠ 0
```

翻转检测（替代脉冲跳变）：

```
if q₀ < 0:
    q_e = -q_e    # 走短路
```

翻转后 `θ_e` 自动改变符号，等价于 so(3) 框架的跳变 `θ_e → -θ_e`。

齐次控制器形式**不变**（在 `ξ = [θ_e; ω_e]` 上），力矩映射也相同。

### 5.3 四元数的工程优势

| | so(3) 指数坐标 | 四元数 |
|---|---|---|
| exp/log | 矩阵指数/对数 | `cos⁻¹` 和归一化 |
| 跳变检测 | `\|θ_e\| = π` | `q₀ < 0`（标量比较） |
| PX4/ROS2 兼容 | 需额外转换 | 原生支持 |
| 理论保证 | Zhou 2023 已证明 | 同构，可直接继承 |

---

## 6. 总结：从"只能用于积分链"到 SO(3) 的桥梁

```
                    齐次控制理论
                    (定义在向量空间 ℝⁿ 上)
                         │
          ┌──────────────┼──────────────┐
          ↓              ↓              ↓
    线性积分链         so(3) ≅ ℝ³     纯虚四元数 ≅ ℝ³
    (直接适用)        (Lie 代数)       (Lie 代数)
    lpc2hpc()         膨胀可定义        膨胀可定义
    lpic2hpic()       Gd ≠ I+μG₀       Gd ≠ I+μG₀
                      (加权膨胀)        (加权膨胀)
                         │              │
                    脉冲系统处理       翻转处理
                    全局歧义            全局歧义
                         │              │
                    力矩映射 M          力矩映射 M
                    ←── Exp ──→      ←── R(q) ──→
                         │              │
                         ↓              ↓
                    物理刚体 (SO(3) 上的实际力矩)
```

**关键结论**：

1. **齐次控制"只能用于积分链"是 `lpc2hpc` 算法的约束，不是齐次理论本身的约束**
2. 齐次理论只需要**状态空间是向量空间**，以便定义膨胀和齐次范数
3. SO(3) 的姿态误差可以通过指数坐标映射到 Lie 代数 so(3) ≅ ℝ³，后者**是向量空间**
4. 全局性通过脉冲系统（或四元数翻转）处理
5. 这在理论上是有严格保证的（Zhou, Polyakov & Zheng 2023）

---

## 参考文献

- Polyakov, A. (2020). _Generalized Homogeneity in Systems and Control_. Springer.
- Zhou, Y., Polyakov, A., & Zheng, G. (2023). Generalized Homogeneous Rigid-Body Attitude Control. _Automatica_ (arXiv:2210.01530v2).
- Wang, S. (2020). _Homogeneous Control: from Theory to Applications_. PhD Thesis, Centrale Lille.
- Polyakov, A. (2019). _HCS Toolbox for MATLAB_ (ver 0.2).
- Bharadwaj, S., et al. (1998). Geometry and inverse optimality in global attitude stabilization. _JGCD_.
- Lee, T., Leok, M., & McClamroch, N.H. (2010). Geometric tracking control of a quadrotor UAV on SE(3). _CDC_.
