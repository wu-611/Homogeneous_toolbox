# 齐次观测器（Homogeneous Observer）原理与实践

> 基于 Polyakov (2020) 理论框架、HCS Toolbox MATLAB 实现、以及王思远 (Wang Siyuan, 2020) 博士论文第 4 章的实验验证。

---

## 目录

1. [从线性观测器到齐次观测器：对偶原理](#1-从线性观测器到齐次观测器对偶原理)
2. [数学结构：齐次观测器的完整定义](#2-数学结构齐次观测器的完整定义)
3. [设计方法 1：对偶升级 lo2ho](#3-设计方法-1对偶升级-lo2ho)
4. [设计方法 2：直接设计 ho_design](#4-设计方法-2直接设计-ho_design)
5. [离散化实现：e_ho 与 si_ho](#5-离散化实现e_ho-与-si_ho)
6. [王思远论文第 4 章详解](#6-王思远论文第-4-章详解)
7. [与齐次控制器的对比](#7-与齐次控制器的对比)
8. [对硕士课题的价值与应用方案](#8-对硕士课题的价值与应用方案)

---

## 1. 从线性观测器到齐次观测器：对偶原理

### 1.1 线性系统的对偶性

在线性系统理论中，控制器设计和观测器设计是**对偶问题**：

| | 控制器 | 观测器 |
|---|---|---|
| **对象** | `ẋ = Ax + Bu` | `ẋ = Ax + Bu, y = Cx` |
| **目标** | 设计 `u = Kx` 使 `A+BK` 稳定 | 设计 `L` 使 `A+LC` 稳定 |
| **对偶系统** | — | 观测器 `(A, C)` ↔ 控制器 `(A', C')` |
| **增益** | `K` 通过 `lqr(A, B, Q, R)` | `L` 通过 `lqr(A', C', Q, R)` 的转置 |

**关键事实**：如果 `{A, B}` 可控，则 `{A', B'}` 可观。反之亦然。

### 1.2 齐次框架下的对偶性

Polyakov 的理论将对偶原理完美地扩展到了齐次框架：

```
lpc2hpc(A, B, K)     →  齐次控制器升级（第 3 章）
lo2ho(A, C, L)       →  齐次观测器升级（第 4 章）

lo2ho(A, C, L) ≡ lpc2hpc(A', C', L') 的转置
```

**MATLAB 实现（`lo2ho.m`）只有一行有效计算**：

```matlab
[L0, G0, P, nu_max, nu_min] = lpc2hpc(A', C', L');
L0 = L0';                    % 转置回来
G0 = -G0';                   % 符号取反（对偶性要求）
nu_min = -nu_min;            % 齐次度符号取反
nu_max = -nu_max;
```

**为什么 `G0` 取负号**：控制器膨胀 `Gd = I + μ·G0_ctl`，观测器膨胀 `Gd = I + ν·G0_obs`。对偶关系要求 `G0_obs = -G0_ctl'`。这是因为观测器处理的是**对偶系统** `(A', C')`，其膨胀生成元与原始系统差一个负号。

**为什么 `ν = -μ`**：控制器的齐次度 μ 定义为 `f(d(s)x) = e^{μs} d(s) f(x)`。观测器误差动力学中，齐次度的符号与控制器相反，因为误差动力学是"收缩"的（衰减到零），而控制动力学是"推动"的（主动施加力）。

---

## 2. 数学结构：齐次观测器的完整定义

### 2.1 系统模型

```
ẋ = A·x + B·u          （被控对象）
y = C·x                 （测量输出）
```

其中 `u(t)` 是**已知的**控制输入信号，`y(t)` 是可测量的输出。

### 2.2 线性 Luenberger 观测器

```
ż = A·z + B·u + L·(C·z - y)
```

误差 `e = z - x` 的动力学：
```
ė = (A + L·C)·e
```

选择 L 使得 `A+LC` 为 Hurwitz → 误差指数收敛。

### 2.3 齐次观测器（Homogeneous Observer, HO）

```
ż = A·z + B·u + [L₀ + |Cz - y|^{ν-1} · expm(ln|Cz - y| · Gd) · (L - L₀)] · (Cz - y)
                 |____________________________________________________________|
                                    非线性增益 g(Cz - y)
```

其中：

| 符号 | 含义 | 来源 |
|------|------|------|
| `L₀` | 齐次化观测器增益 | 使 `A+L₀C` 在最后一块行幂零 |
| `L - L₀` | 非线性观测器增益 | `L` 是线性增益，`L₀` 剥离线性分量 |
| `Gd = I + ν·G₀` | 膨胀生成元 | `G₀` 来自对偶 `lpc2hpc` |
| `ν` | 齐次度 | `ν < 0`：有限时间收敛；`ν > 0`：近固定时间收敛 |
| `|Cz - y|` | 输出误差的欧氏模 | **观测器只能看到的量** |

### 2.4 误差动力学

```
ė = A·e + g(C·e)
  = [A + L₀·C + |Ce|^{ν-1} · expm(ln|Ce| · Gd) · (L-L₀)·C] · e
```

这个误差系统是 **d-齐次的**，齐次度为 ν。

### 2.5 关键差异：为什么观测器用 `|Ce|` 而控制器用 `||x||_d`？

| | 齐次控制器 | 齐次观测器 |
|---|---|---|
| **可用的信息** | 全状态 x | 仅输出误差 `Cz - y = Ce` |
| **缩放依据** | `||x||_d`（状态齐次范数） | `|Ce|`（输出误差的标量模） |
| **信息完备性** | 已知 x → 可以计算 `d(-ln||x||_d)x` | 未知 e → 只能基于 `|Ce|` 缩放 |

这是**信息约束**的直接体现，不是设计选择。观测器永远只能看到 `Cz - y`，所以所有非线性缩放都必须基于这个量。

### 2.6 饱和机制

与控制器一样，观测器也引入了饱和函数 `sat_{a,b}`：

```
g_{a,b}(ε) = sat_{a,b}(|ε|^ν) · expm(ln(sat_{a,b}(|ε|^ν)) · G₀) · L · ε
```

其中 `ε = Cz - y`。

| 参数 | 作用 | 典型值 |
|------|------|--------|
| `a ∈ (0, 1]` | 下界：限制近零点的最大增益（防颤振） | 0.1 - 0.25 |
| `b ∈ [1, +∞)` | 上界：限制远处的最小增益（防发散） | 1 - 2 |

**关键性质**：`g_{1,1}(ε) = L·ε`（退化为线性 Luenberger 观测器）。这意味着齐次观测器的性能**绝不会差于**对应的线性观测器——饱和函数保证了这个下限。

---

## 3. 设计方法 1：对偶升级 `lo2ho`

### 3.1 函数签名

```matlab
[L0, G0, nu_min, nu_max] = lo2ho(A, C, L)
```

### 3.2 输入

| 参数 | 含义 |
|------|------|
| `A ∈ ℝⁿˣⁿ` | 系统矩阵 |
| `C ∈ ℝᵏˣⁿ` | 输出矩阵（满行秩） |
| `L ∈ ℝⁿˣᵏ` | 已有 Luenberger 观测器的增益（`A+LC` 为 Hurwitz） |

### 3.3 输出

| 参数 | 含义 |
|------|------|
| `L₀ ∈ ℝⁿˣᵏ` | 齐次化观测器增益 |
| `G₀ ∈ ℝⁿˣⁿ` | 膨胀生成元基础矩阵 |
| `nu_min` | 容许的最小齐次度 |
| `nu_max` | 容许的最大齐次度 |

### 3.4 使用流程

```matlab
% 1. 已有线性观测器
L_lin = [...]   % Luenberger 增益

% 2. 升级为齐次观测器
[L0, G0, nu_min, nu_max] = lo2ho(A, C, L_lin);

% 3. 选择齐次度
nu = nu_min;    % 取最负值 → 最快有限时间收敛

% 4. 构造膨胀生成元
Gd = eye(n) + nu * G0;
L_nl = L_lin - L0;

% 5. 运行时调用
z = e_ho(h, z, y, A, C, B*u, L0, L_nl, Gd, nu, alpha, beta);
```

### 3.5 demo_lo2ho.m 解读

`demo_lo2ho.m` 展示了一个**完整的输出反馈系统**：

```
系统：旋转倒立摆 (n=4, m=1, k=2)
  测量：吊臂角度 x1 + 旋转臂角度 x2

线性部分：
  观测器：Luenberger 增益 L_lin (4×2)
  控制器：线性反馈 K_lin (1×4)

升级后：
  观测器：HO (nu=-0.5 或其他选择)
  控制器：HPC (mu=-0.5 或其他选择)

仿真循环：
  y = C*x + noise                    % 测量
  u = e_hpc(z, K0, K_nl, ...)       % 齐次控制（使用估计状态 z）
  x = Ah*x + Bh*u                    % 真实对象演化
  z = e_ho(h, z, y, A, C, B*u, ...)  % 齐次观测器更新
```

**实验设置要点**：
- 真实初始状态 `x0 = [2; 2; 1; 2]`，观测器初始估计 `z0 = [0; 0; 0; 0]`——模拟完全不知道初始状态的情况
- 使用 `ZOH` 离散化真实对象（`Ah, Bh`）保证精度
- 观测器使用 `e_ho`（显式离散化）或 `si_ho`（半隐式离散化）更新

---

## 4. 设计方法 2：直接设计 `ho_design`

### 4.1 函数签名

```matlab
[L0, L, Gd, P] = ho_design(A, C, nu, rho)
```

### 4.2 与 `lo2ho` 的区别

| | `lo2ho` | `ho_design` |
|---|---|---|
| **输入** | 已有线性增益 L | 无（从零设计） |
| **原理** | 对偶 `lpc2hpc(A', C', L')` | 对偶 `hpc_design(A', C', nu, rho)` |
| **适用场景** | 已有调好的线性观测器 | 从零开始设计 |
| **实现** | `lpc2hpc(A',C',L')` 的一行封装 | `hpc_design(A',C',-nu,rho)` |

### 4.3 参数 rho

`rho > 0` 是收敛速率调优参数。**rho 越大，收敛越快**。在直接设计中可以通过增大 rho 来加速收敛，但过大可能导致增益过高（类似高增益观测器的问题）。

---

## 5. 离散化实现：`e_ho` 与 `si_ho`

### 5.1 显式离散化 `e_ho`

```matlab
function znew = e_ho(h, z, y, A, C, f, L0, L, Gd, nu, alpha, beta)

nCe = |C*z - y|                            % 输出误差模
nCe = max(alpha, min(beta, nCe))           % 饱和
nCe = max(tol, nCe)                        % 防零

S = L0 + expm(ln(nCe) * (nu*I + Gd - I)) * L   % 非线性增益
znew = (I + h*(A + S*C)) * z + h*f - h*S*y     % 显式 Euler 一步
```

### 5.2 半隐式离散化 `si_ho`

半隐式方法对刚性系统更稳定。`demo_ho.m` 中使用了 `si_ho` 而非 `e_ho`，因为谐波振荡器 `A = [0 1; -1 0]` 在显式 Euler 下不稳定。

### 5.3 选择建议

| 系统类型 | 推荐方法 |
|----------|---------|
| 稳定/弱不稳定系统 | `e_ho`（速度快） |
| 强不稳定/振荡系统 | `si_ho`（更稳定） |
| 实际飞控实现 | ZOH 离散化 + `e_ho` 或 `si_ho` |

---

## 6. 王思远论文第 4 章详解

### 6.1 理论框架：Theorem 4.1.1

**问题**：设计齐次观测器使得误差动力学 `ė = Ae + g(Ce)` 为 d-齐次的且全局渐近稳定。

**核心条件**：

**(a) 结构条件**：`G₀` 需满足
```
A·G₀ = (G₀ + I)·A       （A 的 d-齐次性条件）
C·G₀ = 0                （输出在膨胀下不变）
```

**(b) LMI 条件**：存在 `P ≻ 0, L = P⁻¹C', χ > 0, γ > 0, ν ∈ ℝ` 满足
```
PA + A'P + C'L'P + PLC + 2χP ≺ 0           (4.5a)
(I + νG₀)'P + P(I + νG₀) ≻ 0               (4.5b)
P ≻ γ² C'C                                   (4.5c)
χ²P⁻¹ ≻ Δ(τ)LL'Δ(τ)',  ∀τ ∈ [0, 1/γ]      (4.6)
```

其中 `Δ(τ) = τ(exp(ln(τ^ν)(G₀+I)) - I)`。

**(c) 非线性增益**：
```
g(ε) = |ε|^ν · expm(ln|ε|^ν · G₀) · L · ε    (4.7)
```

当 ν = 0 时，`g(ε) = L·ε`，退化为 Luenberger 观测器。

**(d) 可行性保证**：当 ν 足够接近 0 时，以上 LMI 总是可行的（Proposition 11.1 in Polyakov 2020）。

### 6.2 升级方法：Section 4.2

**算法**：
1. 取已有 Luenberger 观测器增益 `L_lin`
2. 选择 `G₀` 和 `ν` 使得 LMI (4.5)-(4.6) 可行
3. 从 `a = b = 1` 开始（线性观测器）
4. 逐渐增大 `b > 1`，减小 `a < 1`，同时观测估计精度改善
5. 在实际实验中微调直到获得满意的性能

**关键设计原则**：
- 如果线性观测器增益已经最优，齐次观测器的改善可能有限
- 饱和参数保证性能不低于线性版本
- `ν < 0` 时，减小 `a` 等效于增大近零点增益（更快收敛但可能放大噪声）

### 6.3 实际工程案例：QDrone 速度估计（Section 4.3）

这是论文中最重要的应用展示——用齐次观测器替代 Quanser 的原厂滤波器来估计线速度。

**背景**：
- OptiTrack 只提供位置（100Hz）
- IMU 提供加速度（1000Hz）
- **两者采样频率不同步**
- 原厂方案：两个传递函数做数据融合 + 微分

**Wang 的解决方案**：

**Step 1**：将原厂滤波器"翻译"为 Luenberger 观测器

将位置-速度-加速度偏置的动力学建模为：
```
Ẋ = AX + B·aimu_x,   Y = CX + ω = pcam_x

A = [0 1 0; 0 0 1; 0 0 0]    (三阶积分链 + 常值偏置假设)
B = [0; 1; 0]
C = [1, 0, 0]
X = [p_x; v_x; d]             (位置, 速度, 加速度偏置)
```

通过匹配 Bode 图（Fig 4.2），选择 Luenberger 增益：
```
l₁ = 30,  l₂ = 120,  l₃ = 3
```

**Step 2**：将 Luenberger 观测器升级为齐次观测器

```
ν = -0.25
x, y 方向: a = 0.1, b = 2
z 方向:    a = 0.25, b = 1

G₀ = diag(0, 1, 1)   ← 满足 AG₀ = (G₀+I)A 且 CG₀ = 0
```

### 6.4 实验结果

**方案对比**：

| 方案 | 滤波器 | 控制器 |
|------|--------|--------|
| QF+HC | Quanser 原厂滤波器 | 齐次控制器（第 3 章） |
| **HF+HC** | **齐次观测器（第 4 章）** | 齐次控制器（第 3 章） |

**L2 误差改进（Table 4.1）**：

| 通道 | QF+HC | HF+HC | 改进 |
|------|-------|-------|------|
| x | 0.0851 | 0.0760 | **10.7%** |
| y | 0.0491 | 0.0248 | **49.5%** |
| z | 0.0166 | 0.0089 | **46.4%** |
| ψ | 0.0426 | 0.0283 | **33.6%** |

**关键结论**：
- 齐次观测器 + 齐次控制器的组合提供了**超越单独使用齐次控制器的额外改进**
- 最显著的改善在 y 和 z 通道（~50%），这些通道的滤波器性能原较差
- x 通道改善较小（10.7%），可能因为原厂滤波器在 x 方向已经比较优化

### 6.5 工程意义

这段证明了齐次方法不仅是"纸面上的理论"，而且在实际硬件上（Quanser QDrone，基于 Intel Atom 处理器）是可行的：

- 齐次观测器比线性观测器**多消耗 1-1.5% 的 CPU**

  > "the homogeneous controller consumes only 1-1.5% more energy than linear one"（第 7 章结论）

---

## 7. 与齐次控制器的对比

| 维度 | 齐次控制器 (HPC) | 齐次观测器 (HO) |
|------|-----------------|-----------------|
| **升级函数** | `lpc2hpc(A, B, K)` | `lo2ho(A, C, L)` |
| **对偶关系** | 原始系统 | `lpc2hpc(A', C', L')` 的转置 |
| **非线性缩放依据** | `\|\|x\|\|_d`（全状态齐次范数） | `\|Ce\|`（输出误差模） |
| **膨胀生成元** | `Gd = I + μ·G₀` | `Gd = I + ν·G₀` |
| **齐次度符号** | `μ < 0`：有限时间收敛 | `ν < 0`：有限时间收敛 |
| **Lyapunov 函数** | `V(x) = \|\|x\|\|_d` | `V(e) = \|\|e\|\|_d` |
| **饱和函数** | `sat_{α,β}(\|\|x\|\|_d)` | `sat_{a,b}(\|Ce\|)` |
| **在线计算量** | 计算 `hnorm` + `expm` | 计算 `\|Ce\|` + `expm(log(\|Ce\|)·Gd)` |
| **实时性** | 需要二分法求齐次范数 | **不需要二分法！直接用标量** |
| **已知必要条件** | 系统可控 | 系统可观 |
| **P 矩阵来源** | Lyapunov 方程 | 对偶 Lyapunov 方程 |
| **对饱和的依赖** | 需要 | 需要 |
| **信息需求** | 全状态 x | 仅测量 y（输出反馈） |

### 7.1 关键差异的深层原因

**为什么观测器不需要二分法求齐次范数？**

控制器需要 `||x||_d` 因为它是**全状态**的函数——需要用一个标量来概括多维状态的"大小"。这需要求解隐式方程 `y'Py = 1`。

观测器只需要 `|Ce|`——这是**输出误差**的直接标量模，不需要二分法。计算量显著更小。

**哪个更"重"？**

| 操作 | 控制器 | 观测器 |
|------|:------:|:------:|
| 二分法求范数 | ✓（每步迭代 20 次 expm） | ✗ |
| 输出误差模 | — | ✓（一次向量模） |
| 膨胀`expm(ln(·)·Gd)` | ✓ | ✓ |
| 矩阵向量乘法 | ✓ | ✓ |
| **总计算量** | 较高 | 较低 |

---

## 8. 对硕士课题的价值与应用方案

### 8.1 直接的应用场景

在你的 SE(3) 几何齐次控制器中，位置回路需要 `[e_x, e_y, e_z, e_vx, e_vy, e_vz]` 六个状态。其中：
- **位置误差**：从 GPS/OptiTrack 直接可得
- **线速度误差**：不一定能直接测量，或测量噪声大

**齐次观测器正好解决线速度估计问题**：

```
测量：位置 x, y, z（来自 GPS/OptiTrack）
已知输入：期望加速度 + 推力方向 + 重力补偿
估计：位置 + 线速度 + 可能的外扰偏置

每个通道独立：
  A_ch = [0 1; 0 0]    (双积分器)
  C_ch = [1 0]          (只测位置)
  L_ch = 已有的线性观测器增益
  → lo2ho(A_ch, C_ch, L_ch) → 齐次观测器
```

### 8.2 论文中的定位建议

**方案 A：作为核心贡献的一部分（推荐）**

论文标题可调整为：
> "Output-Feedback Homogeneous Geometric Tracking Control of a Quadrotor on SE(3)"

贡献陈述：
1. 提出 SE(3) 上的**输出反馈**齐次几何控制器
2. 位置回路：齐次观测器 + 齐次控制器（输出反馈，不需线速度测量）
3. 姿态回路：全状态齐次反馈（IMU 直接提供姿态+角速度）
4. 复合系统的级联稳定性分析
5. 仿真及 SITL 实验验证

**方案 B：作为扩展章节**

如果主贡献是"全状态反馈 SE(3) 齐次控制器"，则观测器作为第 5 章扩展：
- 指出全状态反馈在实际中需要速度估计
- 展示齐次观测器的设计和实验验证
- 证明复合系统（HO + HPC）的稳定性

### 8.3 稳定性分析注意事项

**齐次系统的分离原理不一定成立！**

在线性系统中，观测器和控制器可以独立设计（分离原理）。但在齐次系统中：
- 控制器 `u(z)` 使用**估计状态** z，而非真实状态 x
- 闭环系统同时包含控制器非线性 + 观测器非线性
- 不能简单地将两个 Lyapunov 函数相加（交叉项不自动消去）

**Wang 的论文也没有证明复合系统的理论稳定性**——他只通过实验验证了"HO + HC"的性能优于"线性滤波器 + HC"。这是一个开放的研究问题。

**你的处理方案**：
1. 如果时间充裕：尝试证明复合齐次系统的 ISS 稳定性（这是一项真正的理论贡献，可以发好期刊）
2. 如果时间有限：参考 Wang 的做法，用仿真 + SITL 实验验证复合系统的经验性能，将理论证明留作 future work
3. 中间路线：证明在"观测器收敛速度远快于控制器"的条件下（时间尺度分离），复合系统保持稳定

### 8.4 Python 实现清单

你的 `hcs_toolbox_py` 中目前**没有**观测器相关函数。需要移植的：

| MATLAB | Python | 优先级 |
|--------|--------|:------:|
| `lo2ho.m` | `lo2ho.py` | 高 |
| `e_ho.m` | `e_ho.py` | 高 |
| `si_ho.m` | `si_ho.py` | 中 |
| `ho_design.m` | `ho_design.py` | 低 |
| `block_obs.m` | `block_obs.py` | 低 |
| `trans_obs.m` | `trans_obs.py` | 低 |

**`lo2ho.py` 的实现就是一行**：

```python
def lo2ho(A, C, L):
    K0, G0, P, nu_max, nu_min = lpc2hpc(A.T, C.T, L.T)
    return K0.T, -G0.T, -nu_min, -nu_max
```

`e_ho.py` 已经在上方给出了伪代码，可以直接翻译。

### 8.5 时间评估

| 工作 | 时间 |
|------|------|
| 移植 `lo2ho` + `e_ho` 到 Python | 2-3 天 |
| 位置回路输出反馈仿真 | 1-2 周 |
| 姿态回路保持全状态反馈 | 已有代码 |
| 复合系统稳定性分析（可选） | 1-3 月 |
| SITL 实验验证 | 1-2 月 |

---

## 参考文献

1. Wang, S. (2020). _Homogeneous Quadrotor Control: Theory and Experiment_. PhD Thesis, Centrale Lille. Chapter 4.
2. Polyakov, A. (2020). _Generalized Homogeneity in Systems and Control_. Springer. Chapter 11.
3. Polyakov, A. (2019). _HCS Toolbox for MATLAB_ (ver 0.2).
4. Lopez-Ramirez, F., Polyakov, A., Efimov, D., & Perruquetti, W. (2018). Finite-time and fixed-time observer design: Implicit Lyapunov function approach. _Automatica_, 87:52-60.
