# 齐次控制从 ℝⁿ 积分链到 SO(3) 姿态控制的扩展：详细解析

> 核心参考文献：Zhou, Y., Polyakov, A. & Zheng, G. (2023). _Generalized Homogeneous Rigid-Body Attitude Control_. IEEE Transactions on Automatic Control (arXiv:2210.01530v2).

---

## 目录

1. [前置知识：齐次控制为何要求积分链](#1-前置知识齐次控制为何要求积分链)
2. [问题：为何不能直接在 SO(3) 上定义齐次控制](#2-问题为何不能直接在-so3-上定义齐次控制)
3. [突破口：Lie 代数 so(3) 是向量空间](#3-突破口lie-代数-so3-是向量空间)
4. [关键概念 1：指数坐标与姿态误差](#4-关键概念-1指数坐标与姿态误差)
5. [关键概念 2：右 Jacobian 与误差动力学](#5-关键概念-2右-jacobian-与误差动力学)
6. [关键概念 3：为何需要脉冲系统](#6-关键概念-3为何需要脉冲系统)
7. [脉冲系统的完整定义](#7-脉冲系统的完整定义)
8. [脉冲齐次系统的稳定性理论（Theorem 2）](#8-脉冲齐次系统的稳定性理论theorem-2)
9. [姿态齐次控制器的设计（Theorem 3）](#9-姿态齐次控制器的设计theorem-3)
10. [控制器参数的逐项解释](#10-控制器参数的逐项解释)
11. [Lyapunov 证明的要点](#11-lyapunov-证明的要点)
12. [跳变边界的验证](#12-跳变边界的验证)
13. [固定时间控制（Corollary 1）](#13-固定时间控制corollary-1)
14. [从虚拟控制到实际力矩的映射](#14-从虚拟控制到实际力矩的映射)
15. [与标准 lpc2hpc 的关键差异对比](#15-与标准-lpc2hpc-的关键差异对比)
16. [扩展到四元数表示](#16-扩展到四元数表示)
17. [总结](#17-总结)

---

## 1. 前置知识：齐次控制为何要求积分链

### 1.1 齐次控制的三个基石

齐次控制建立在三个数学对象上：

**基石 1：膨胀群（dilation group）**
```
d(s) = expm(s · Gd),   s ∈ ℝ
```
`d(s)` 是 ℝⁿ 上的**线性变换**。它将状态向量 x 缩放为 `d(s)x`。`Gd` 是膨胀生成元（anti-Hurwitz 矩阵）。

**基石 2：齐次范数（homogeneous norm）**
```
||x||_d = exp(c),   c 满足:  y'Py = 1,  其中 y = expm(-c·Gd)·x
```
这需要 `P ≻ 0` 且 `PGd + Gd'P ≻ 0`（保证膨胀的单调性）。

**基石 3：齐次控制律**
```
u = K₀·x + ||x||_d^(1+μ) · (K - K₀) · expm(-ln||x||_d · Gd) · x
```
每一项都是 ℝⁿ 上的向量运算。

### 1.2 为什么 lpc2hpc 算法要求块可控型（积分链）

`lpc2hpc(A, B, K)` 算法的**步骤 2** 要求构造 `K₀` 使得 `A₀ = A + BK₀` 在最后一块行幂零。这在数学上等价于要求 {A, B} 可以通过相似变换 `T` 化为**块可控典范型**：

```
TAT⁻¹ = [0    I    0   ...  0 ]
        [0    0    I   ...  0 ]
        [...                 I ]
        [*    *    *   ...  * ]

TB   = [0; 0; ...; I]
```

这本质上是一个**积分链**：`ẋ₁ = x₂, ẋ₂ = x₃, ..., ẋₖ = u`。只有在积分链结构上，膨胀生成元才能具有 `diag([k-1, ..., 0])` 的规范形式。

**但这是 `lpc2hpc` 算法的要求，而非齐次理论本身的要求。** 齐次理论只需要状态空间是向量空间。

---

## 2. 问题：为何不能直接在 SO(3) 上定义齐次控制

### 2.1 SO(3) 的数学结构

SO(3) 是所有 3×3 旋转矩阵的集合：

```
SO(3) = {R ∈ ℝ³ˣ³ : R Rᵀ = I, det(R) = 1}
```

SO(3) 是一个**李群（Lie group）**，也是一个**三维紧流形**。它不是向量空间：
- 两个旋转矩阵的和不一定是旋转矩阵：`R₁ + R₂ ∉ SO(3)`
- 旋转矩阵没有"缩放"操作：不存在 `α · R` 对 `α ≠ ±1`

### 2.2 膨胀在 SO(3) 上的不可能性

膨胀 `d(s)` 必须是 ℝⁿ 上的**线性变换**。SO(3) 上的"对应物"应该满足：

```
D(s) : SO(3) → SO(3),   D(s₁)∘D(s₂) = D(s₁+s₂),   D(0) = id
```

且要求 `D(s)` 能产生有意义的"缩放"——大 s 使点远离恒等元，小 s 使点靠近恒等元。

**定理（微分几何基本事实）**：紧李群上不存在非平凡的膨胀群作用。SO(3) 是紧的，其上任何连续单参数自同构群只能是共轭作用 `R ↦ Q R Qᵀ`——这不改变旋转角，只是旋转坐标轴。

**物理直觉**：在 SO(3) 上，你无法"把 180° 的旋转放大到 360°"——旋转角的取值被紧性限制在 [0, π] 区间内。

### 2.3 刚体姿态动力学回顾

全驱动刚体的旋转动力学（惯性系，体坐标系角速度）：

```
Ṙ = R ω^                                    (1a)
ω̇ = J⁻¹(-ω × Jω + M)                       (1b)
```

其中：
- `R(t) ∈ SO(3)`：旋转矩阵（刚体相对于惯性系）
- `ω(t) ∈ ℝ³`：体坐标系下的角速度
- `J ∈ ℝ³ˣ³`：转动惯量矩阵
- `M(t) ∈ ℝ³`：体坐标系下的力矩（**控制输入**）
- `ω^` 是 `ω` 的反对称矩阵：`ω^ = [0, -ω₃, ω₂; ω₃, 0, -ω₁; -ω₂, ω₁, 0]`

直接对 R 状态应用齐次控制？不可能——R 不是向量空间的元素。

---

## 3. 突破口：Lie 代数 so(3) 是向量空间

### 3.1 so(3) 的定义

so(3) 是 SO(3) 在恒等元处的切空间，由所有 3×3 反对称矩阵组成：

```
so(3) = {X ∈ ℝ³ˣ³ : Xᵀ = -X}
```

任意 `X ∈ so(3)` 可以唯一表示为 `X = x^`，其中 `x ∈ ℝ³`。因此 **so(3) 与 ℝ³ 是线性同构的**：

```
x = [x₁, x₂, x₃]ᵀ  ↦  x^ = [  0   -x₃   x₂ ]
                            [ x₃    0   -x₁ ]
                            [-x₂   x₁    0  ]

X ↦ Xˇ = [X₃₂, X₁₃, X₂₁]ᵀ   (逆映射)
```

### 3.2 指数映射：从 so(3) 到 SO(3)

**指数映射（exponential map）**将 Lie 代数映射到 Lie 群：

```
exp : so(3) → SO(3)
Exp : ℝ³ → SO(3),   Exp(θ) = exp(θ^)
```

**Rodrigues 公式**给出闭式表达式：

```
Exp(θ) = I₃ + (sin|θ|/|θ|) · θ^ + ((1-cos|θ|)/|θ|²) · θ^2
```

物理含义：`θ = φ · n`，其中 `φ ∈ [0, π]` 是旋转角，`n` 是单位旋转轴。`Exp(θ)` 给出绕轴 n 旋转 φ 弧度的旋转矩阵。

### 3.3 对数映射：从 SO(3) 回 so(3)

**对数映射（logarithmic map）**是指数映射的逆：

```
Log : SO(3) → ℝ³,   Log(R) = (log(R))ˇ
```

对于 `tr(R) ≠ -1`（即旋转角 φ ≠ π）：

```
Log(R) = (φ / (2 sin φ)) · (R - Rᵀ)ˇ
```

其中 `φ = arccos((tr(R) - 1) / 2)`。

### 3.4 核心观察

`Exp` 和 `Log` 建立了 SO(3) 和 ℝ³ 中半径为 π 的球体之间的桥梁：

```
Exp : B³[π] = {θ ∈ ℝ³ : |θ| ≤ π} → SO(3)    (满射)
Log : SO(3) → B³[π]                           (分段双射)
```

**这就是关键**：姿态（SO(3) 的元素）可以通过指数坐标 θ ∈ ℝ³ 来表示。而 ℝ³ **是向量空间**——可以定义膨胀、齐次范数、齐次控制器。

---

## 4. 关键概念 1：指数坐标与姿态误差

### 4.1 姿态跟踪误差

给定当前姿态 R(t) 和期望姿态 R_d(t)，定义姿态误差：

```
R_e = R R_dᵀ = Exp(θ_e)                                (17)
```

其中 `θ_e ∈ B³[π]` 是指数坐标下的姿态误差。**θ_e 是 ℝ³ 中的向量**。

### 4.2 为什么 θ_e 能表示所有姿态误差

- 旋转角 `|θ_e|` 在 [0, π] 范围内
- 旋转轴 `θ_e/|θ_e|` 在 S² 上
- 当 `|θ_e| = π` 时：`θ_e` 和 `-θ_e` 对应同一个旋转矩阵（Exp(πn) = Exp(-πn)）

### 4.3 歧义问题

在 `|θ_e| = π` 的球面上，Log 映射出现歧义：

```
Exp(πn) = Exp(-πn)    ← 同一个旋转矩阵，两个不同的 θ_e
```

这是 SO(3) 拓扑的必然结果（SO(3) 的 double cover 性质）。论文通过**脉冲系统**处理这个歧义（见第 6 节）。

---

## 5. 关键概念 2：右 Jacobian 与误差动力学

### 5.1 θ_e 的微分方程

指数坐标误差的导数与角速度误差的关系（Chirikjian, 2012）：

```
ė_e = J_r⁻¹(θ_e) · ω_e                               (18)
```

其中 `J_r(θ_e)` 是 SO(3) 的**右 Jacobian**。

### 5.2 角速度误差

```
ω_e = R_d (ω - ω_d)    （体坐标系下的角速度误差）
ω_d = (R_dᵀ Ṙ_d)ˇ       （期望姿态的体坐标系角速度）
```

### 5.3 右 Jacobian 的表达式

`J_r` 的显式表达（Chirikjian, 2012）：

```
J_r(θ^) = I - ((1-cos|θ|)/|θ|²) · θ^ + ((|θ|-sin|θ|)/|θ|³) · (θ^)²

J_r⁻¹(θ^) = I + (1/2) · θ^ + (1/|θ|² - (1+cos|θ|)/(2|θ|sin|θ|)) · (θ^)²   (38)
```

### 5.4 J_r 的关键性质

| 性质 | 含义 |
|------|------|
| `J_r(0) = I₃` | 在小角度下，`ė_e ≈ ω_e`（像双积分器） |
| `J_r⁻¹` 在 `\|θ_e\| ≤ π` 上有界 | 保证 Lyapunov 分析中的不等式放大有效 |
| `θ_e' · J_r⁻¹(θ_e) · ω_e = θ_e' · ω_e` | 论文 Lemma (27)——一个简洁但重要的恒等式 |

**第三个性质**对稳定性证明至关重要：尽管 `J_r⁻¹` 的结构复杂，但在关键的内积项中，它退化为单位矩阵。这意味着**脉冲系统条件 (9) 和 (10) 的验证可以被大幅简化**。

---

## 6. 关键概念 3：为何需要脉冲系统

### 6.1 拓扑障碍

SO(3) 上不存在连续时不变全局渐近稳定反馈控制（Bullo & Murray, 1995）。根本原因是 SO(3) 的拓扑：它是一个不可收缩的紧流形。

### 6.2 在指数坐标中的表现

指数坐标的**值域**是闭球 `B³[π] = {θ_e : |θ_e| ≤ π}`：

```
B³(π) = {θ_e : |θ_e| < π}    (开球，内部)
S²(π) = {θ_e : |θ_e| = π}    (球面，边界)
```

- 在 `B³(π)` 内部：Log 映射是**双射**——每个 R_e 对应唯一的 θ_e
- 在 `S²(π)` 边界上：Exp(πn) = Exp(-πn)——歧义发生

### 6.3 脉冲跳变机制

当系统轨迹到达球面边界 `|θ_e| = π` 且试图穿出球体时（`d/dt|θ_e| > 0`），必须将状态**重置**到球面上的对径点：

```
θ_e → -θ_e    （跳变）
```

对应的映射矩阵：

```
Π = [-I₃   0 ]
    [ 0    I₃]

即  [θ_e; ω_e] → [-θ_e; ω_e]
```

### 6.4 物理含义

|θ_e| > π 对应一个"超过 180°"的旋转角，这在指数坐标中没有直接的对应点。跳变相当于说："不要走超过 180° 的大圆弧，而是走反方向的小圆弧"。这类似于四元数中的 `q → -q` 翻转。

---

## 7. 脉冲系统的完整定义

论文将姿态误差动力学表述为以下脉冲系统（公式 (19)）：

```ocaml
连续演化 (ξ ∈ C):
    ξ̇ = [0   J_r⁻¹(θ_e)] ξ + [0] u        (19a)
         [0      0     ]      [I₃]

跳变 (ξ ∈ D):
    ξ = Π ξ⁻                                (19b)
```

其中：

| 符号 | 含义 |
|------|------|
| `ξ = [θ_e; ω_e] ∈ ℝ⁶` | 系统状态（6 维向量空间！） |
| `C = B³(π) × ℝ³` | **流动集**（flow set）：状态连续演化的区域 |
| `D = {ξ : \|θ_e\|=π, θ_e'ω_e>0}` | **跳变集**（jump set）：触发跳变的边界 |
| `Π = [-I₃, 0; 0, I₃]` | 跳变映射（翻转 θ_e，保持 ω_e） |
| `u = ω̇_e` | **虚拟控制**：角加速度（待设计） |
| `B = [0; I₃]` | 控制输入矩阵 |

关键：
- **流动集 C** 是一个开连通集，包含原点。在 C 内部，Exp/Log 是双射，系统正常演化。
- **跳变集 D** 是 ∂C 的子集（球面边界 + 试图穿出的速度方向）。状态到达 D 时立即跳变，进入 C 继续演化。
- `ΠD ⊂ ∂C \ D`：跳变后状态不落在 D 中（避免无限跳变循环）。

---

## 8. 脉冲齐次系统的稳定性理论（Theorem 2）

这是论文对理论的**核心贡献之一**——将齐次 Lyapunov 函数方法扩展到一类特定的脉冲系统。

### 8.1 定理陈述

考虑脉冲系统 `ẋ = f(t, x)`（`x ∈ C`），`x = Πx⁻`（`x⁻ ∈ D`），其中 `C` 和 `D` 如 (8) 定义。

**如果**：

**(i)** 在无脉冲时，典范齐次范数 `V(x) = ||x||_d` 满足 Lyapunov 不等式：

```
∂V/∂x · f(t, x) ≤ -ρ · V^(1+μ)(x),   ∀x ∈ C\{0}          (11)
```

**(ii)** 膨胀与跳变交换（commutation）：

```
Gd · Π = Π · Gd                                           (12)
```

**(iii)** 跳变时 Lyapunov 函数不增加：

```
y'Π'PΠy ≤ 1,   ∀y ∈ π_d(D)                               (13)
```

其中 `π_d(D)` 是跳变集 D 在单位球面上的齐次投影。

**(iv)** 向量场在边界上指向内部：

```
∂g₁/∂x · f(t,x) < 0,   ∀x ∈ ∂C\D                          (9)
∂g₁/∂x · f(t,x) = 0,   ∀x ∈ ∂D                            (10a)
∂g₂/∂x · f(t,x) ≤ -γ,  ∀x ∈ ∂D ∩ Bⁿ[r]                    (10b)
```

**则**：

| μ | 稳定性结论 |
|---|-----------|
| μ < 0 | 全局一致有限时间稳定 |
| μ = 0 | 全局一致指数稳定 |
| μ > 0 | 全局一致近似固定时间稳定 |

### 8.2 条件 (12) 的验证：膨胀与跳变交换

```
Gd = [(1-μ)I₃     0   ]    Π = [-I₃   0 ]
     [   0        I₃  ]        [ 0    I₃]

Gd · Π = [(1-μ)I₃     0   ] · [-I₃   0 ] = [-(1-μ)I₃    0   ]
         [   0        I₃  ]   [ 0    I₃]   [   0        I₃  ]

Π · Gd = [-I₃   0 ] · [(1-μ)I₃     0   ] = [-(1-μ)I₃    0   ]
         [ 0    I₃]   [   0        I₃  ]   [   0        I₃  ]

∴ Gd · Π = Π · Gd   ✓
```

块对角结构 + 每个块都是单位阵的倍数 → **自动成立**。

**推论**：`expm(s·Gd)·Π = Π·expm(s·Gd)`，即 `d(s)Π = Πd(s)`。这意味着"先跳变再膨胀"与"先膨胀再跳变"是等价的。

### 8.3 条件 (13) 的验证：Lyapunov 函数在跳变中不增

对于论文的姿态控制设定（Remark 1），使用分块结构：

```
P = [p₁₁I₃  p₁₂I₃]
    [p₁₂I₃  p₂₂I₃]
```

跳变后：`Πξ = [-θ_e; ω_e]`。计算：

```
(Πξ)'P(Πξ) = p₁₁|θ_e|² - 2p₁₂ θ_e'ω_e + p₂₂|ω_e|²
           ≤ p₁₁|θ_e|² + 2p₁₂ θ_e'ω_e + p₂₂|ω_e|²   (因 θ_e'ω_e > 0 在 D 上)
           = ξ'Pξ
```

所以 `||Πξ||_d ≤ ||ξ||_d`——**跳变不增加 Lyapunov 函数值**。✓

---

## 9. 姿态齐次控制器的设计（Theorem 3）

### 9.1 控制律

```
u_hom = ||ξ||_d^(1+μ) · K · d(-ln||ξ||_d) · ξ          (20)
```

其中 `K = [-K₁  -k₂I₃]`，`K₁ ≻ 0`，`k₂ > 0`。

展开形式：

```
u_hom = -||ξ||_d^(1+μ) · [K₁ · d_θ(-ln||ξ||_d)·θ_e + k₂ · d_ω(-ln||ξ||_d)·ω_e]
```

注意：这与标准 `lpc2hpc` 给出的控制律形式（包含 `K₀` 的线性项和 `K-K₀` 的非线性项）在表面上有差异。这里的控制律**直接使用齐次形式**，没有显式的 `K₀` 分解。两者在数学上是等价的（通过适当的 K 分解），但 so(3) 版本的形式更简洁。

### 9.2 膨胀生成元 Gd

```
Gd = [(1-μ)I₃     0   ]                                 (21a)
     [   0        I₃  ]
```

**这是与积分链齐次控制最关键的差异**：

| | 标准 lpc2hpc（n 阶积分链）| so(3) 齐次控制 |
|---|---|---|
| Gd 结构 | `diag([k-1, k-2, ..., 0]) ⊗ I_m` | `[(1-μ)I₃, 0; 0, I₃]` |
| Gd 来源 | 由块可控维数决定 | 人为设计的加权膨胀 |
| Gd 对角元素 | 递减整数 | 1-μ 和 1 |

对于积分链，膨胀权重反映状态间"积分"的层次关系——位置（积分 2 次）的膨胀权重比速度（积分 1 次）大，速度的膨胀权重比加速度大。

对于 SO(3) 姿态，`|θ_e| ≤ π` 是有界的，不存在"无限积分"的概念。因此膨胀权重被选为**标量 (1-μ) 和 1**。`θ_e`（旋转角误差）需比 `ω_e`（角速度误差）有更大的膨胀权重（当 μ<0, 1-μ>1），以确保姿态误差优先被压缩。

### 9.3 形状矩阵 P

```
P = [  I₃     εI₃  ]                                    (21b)
    [ εI₃     K₁⁻¹ ]
```

P 需要满足两个条件：
1. `P ≻ 0`（正定）
2. `PGd + Gd'P ≻ 0`（膨胀单调性）

这两个条件转化为对 ε 的约束：

```
ε < ε_μ = 2√(1-μ) / ((2-μ)√λ_max(K₁))                  (P ≻ 0)
ε < ε̃   = 4k₂² / (c·λ_max(K₁) + k₂²)                   (W ≺ 0)
```

最终选择：`ε ∈ (0, min{ε_μ, ε̃})`。

**物理含义**：ε 控制 `θ_e` 和 `ω_e` 在 Lyapunov 函数中的耦合程度。如果 ε 太大，Lyapunov 导数可能不正定（当姿态误差大时，Jacobian 的耦合效应可能破坏负定性）；如果 ε 太小，P 不正定（块对角占优条件被破坏）。

其中：

```
c = max_{|θ_e| ≤ π} λ_max(J_r⁻¹(θ_e) + J_r⁻ᵀ(θ_e))
```

c 量化了右 Jacobian 偏离恒等矩阵的最大程度。在 `|θ_e| ≤ π` 上，`J_r⁻¹` 有界，所以 c 是有限值。

### 9.4 收敛率 ρ̃

```
ρ̃ = λ_min( P^(-1/2) · [2εK₁      εK₂          ] · P^(-1/2) )
                       [εK₂   K₁⁻¹K₂+K₂K₁⁻¹-εcI₃]
    /
    λ_max( P^(1/2)·Gd·P^(-1/2) + P^(-1/2)·Gd'·P^(1/2) )
```

分子来自 Lyapunov 导数放缩的负定矩阵 W，分母来自齐次范数导数中的归一化因子 `PGd + Gd'P`。

---

## 10. 控制器参数的逐项解释

### 10.1 增益 K = [-K₁  -k₂I₃]

这是一个类似 PD 控制器的结构：
- `K₁`：比例增益（作用于姿态误差 θ_e）
- `k₂`：阻尼增益（作用于角速度误差 ω_e）

论文中取 `K₁ = k_p I₃`，`k₂ = k_d`，即各通道解耦。

### 10.2 K₁ 和 k₂ 的选择

论文仿真中取 `K₁ = 12I₃`，`k₂ = 6`。选择依据：
- 力矩限制在 `[-0.5, 0.5]` N·m
- 自然频率 ≈ 2 rad/s，阻尼比 ≈ 0.87

### 10.3 μ 的范围

`μ ∈ [-1, 1)`：
- μ = -1：最大有限时间收敛速率
- μ = 0：退化为经典指数坐标上的线性 PD 控制器 `u = -K₁θ_e - k₂ω_e`
- μ → 1：接近固定时间收敛

### 10.4 ε 的物理意义

ε 在 P 矩阵中的非对角块，决定 `θ_e` 与 `ω_e` 在 Lyapunov 函数中的耦合。可以理解为一种"交叉权重"——确保姿态误差和角速度误差在齐次范数中被同时压缩。

---

## 11. Lyapunov 证明的要点

论文使用 `V(ξ) = ||ξ||_d`（典范齐次范数本身）作为 Lyapunov 函数。

### 11.1 齐次范数的导数

对于无脉冲区域 `ξ ∈ C`：

```
d/dt ||ξ||_d = ||ξ||_d · (ξ'd'(-s_ξ)P d(-s_ξ)ξ̇) / (ξ'd'(-s_ξ)PGd d(-s_ξ)ξ)
```

其中 `s_ξ = ln||ξ||_d`。

### 11.2 核心化简

利用膨胀的齐次性质：

```
d(-s_ξ) ξ = [||ξ||_d^(μ-1)·θ_e;  ||ξ||_d^(-1)·ω_e]
d(-s_ξ) ξ̇ = [||ξ||_d^(μ-1)·ė_e;  ||ξ||_d^(-1)·ė_ω]
           = [||ξ||_d^(μ-1)·J_r⁻¹·ω_e;  ||ξ||_d^(-1)·u_hom]
```

代入 P 的分块结构，展开后利用 `θ_e' J_r⁻¹ ω_e = θ_e' ω_e`（等式 (27)），得到：

```
V̇ = ||ξ||_d^(1+μ) · (ξ' d'(-s_ξ) W₀ d(-s_ξ) ξ) / (ξ' d'(-s_ξ)(PGd+Gd'P) d(-s_ξ) ξ)
```

其中 `W₀` 依赖于 `J_r⁻¹`。利用 `J_r⁻¹` 的有界性（c 的定义），进一步放缩：

```
V̇ ≤ -ρ̃ · ||ξ||_d^(1+μ)      在 C\{0} 内                        (29)
```

### 11.3 结合脉冲跳变完成证明

由条件 (13)，跳变时 V 不增：
```
V(ξ(t_k)) ≤ V(ξ⁻(t_k))
```

结合连续段上的衰减 (29)，对 `μ ≠ 0`：
```
d/dt(V^(-μ)) ≤ ρ̃μ
```

积分得到有限时间/固定时间收敛估计。

---

## 12. 跳变边界的验证

论文定义：

```
g₁(ξ) = θ_e'θ_e - π²     （球面边界条件）
g₂(ξ) = ω_e'θ_e          （穿出方向条件）
```

则：
- `C = {ξ : g₁(ξ) < 0}`——球内部
- `D = {ξ : g₁(ξ) = 0, g₂(ξ) > 0}`——球面上且向外穿出

### 12.1 验证条件 (9)：向量场在 ∂C\D 上指向内部

```
∂C\D = {ξ : |θ_e| = π, θ_e'ω_e < 0}

∂g₁/∂ξ · f(ξ) = 2θ_e' · J_r⁻¹·ω_e = 2θ_e'ω_e < 0    （在 ∂C\D 上）
```

所以向量场将状态推回球内。✓

### 12.2 验证条件 (10a)：在 ∂D 上 g₁ 的导数为零

```
∂D = {ξ : |θ_e| = π, θ_e'ω_e = 0}

∂g₁/∂ξ · f(ξ) = 2θ_e'ω_e = 0    （在 ∂D 上）
```

∂D 上的点处于"恰好不穿出也不穿入"的临界状态。✓

### 12.3 验证条件 (10b)：在 ∂D 上 g₂ 的导数为负

```
∂g₂/∂ξ · f(ξ) = 2ω_e'J_r⁻¹ω_e + 2||ξ||_d^(μ+1)·θ_e'·K·d(-s_ξ)·ξ
```

利用 `θ_e'ω_e = 0` 和 `J_r⁻¹` 的性质（式 (38) 展开），可以证明：

```
ω_e'J_r⁻¹ω_e = |ω_e|² + ω_e'(θ_e^/|θ_e|)²ω_e
            = |ω_e|² + ω_e'(θ_e θ_e'/|θ_e|²)ω_e    （因 (θ_e^)² = θ_e θ_e' - |θ_e|²I）
            = |ω_e|² + (θ_e'ω_e)²/|θ_e|²
            = |ω_e|²    （因 θ_e'ω_e = 0）
∂g₂/∂ξ · f(ξ) = 2|ω_e|² - 2||ξ||_d^(2μ)·θ_e'K₁θ_e < 0 - 正数 < 0
```

所以状态在 ∂D 上向 D 的补集移动（不会在边界上卡住）。✓

---

## 13. 固定时间控制（Corollary 1）

### 13.1 基本思想

单一齐次控制器只能保证：
- μ < 0：有限时间收敛（收敛时间依赖于初值）
- μ > 0：近固定时间收敛（收敛到零的邻域，但不精确到零）

要实现**精确的固定时间收敛到零**（收敛时间上界与初值无关），采用**切换策略**：

```
u_fxt = {
    ||ξ||_d₁^(1+μ₁) · K · d₁(-ln||ξ||_d₁) · ξ,    ξ'Pξ ≥ 1    (μ₁ > 0)
    ||ξ||_d₂^(1+μ₂) · K · d₂(-ln||ξ||_d₂) · ξ,    ξ'Pξ ≤ 1    (μ₂ < 0)
}
```

### 13.2 工作原理

```
阶段 1 (μ₁ > 0):  从任意初始状态 → 固定时间 T₁ = 1/(ρ̃₁μ₁) 内进入单位球 {ξ'Pξ ≤ 1}
阶段 2 (μ₂ < 0):  在单位球内 → 有限时间 T₂ = 1/(-ρ̃₂μ₂) 内精确收敛到零

总时间 ≤ T₁ + T₂，与初值无关。
```

### 13.3 连续性的保证

切换面 `ξ'Pξ = 1` 上：
- `||ξ||_d₁ = ||ξ||_d₂ = 1`（由 P 矩阵相同保证）
- `d₁(0) = d₂(0) = I₆`
- 因此 `u_fxt` 在切换面上是连续的

---

## 14. 从虚拟控制到实际力矩的映射

### 14.1 完整力矩计算公式

```
M = J [R_dᵀ · u_hom - ω_d^× · ω + ω̇_d] + ω × Jω              (23)
```

### 14.2 推导过程

1. `u_hom = ω̇_e`（虚拟控制，即误差角加速度）
2. 回顾 `ω_e = R_d(ω - ω_d)`：
   ```
   ω̇_e = R_d ω_d^×(ω - ω_d) + R_d(ω̇ - ω̇_d)               (24)
        = R_d ω_d^× ω - R_d ω_d^× ω_d + R_d ω̇ - R_d ω̇_d
        = R_d ω_d^× ω + R_d ω̇ - R_d ω̇_d    （因 ω_d^× ω_d = 0）
   ```
3. 其中 `ω̇_d = (R_dᵀ R̈_d - (ω_d^×)²)ˇ`
4. 从 (1b) 解出 `ω̇ = J⁻¹(-ω×Jω + M)`
5. 代入并令 `ω̇_e = u_hom`：
   ```
   u_hom = R_d ω_d^× ω + R_d J⁻¹(-ω×Jω + M) - R_d ω̇_d
   ```
6. 解出 M：
   ```
   J⁻¹ M = R_dᵀ u_hom - ω_d^× ω + ω̇_d + J⁻¹ ω×Jω
   M = J(R_dᵀ u_hom - ω_d^× ω + ω̇_d) + ω×Jω
   ```

### 14.3 各项的物理含义

| 项 | 含义 |
|----|------|
| `J·R_dᵀ·u_hom` | 将 so(3) 上的齐次控制映射为惯性系力矩，再转换到体坐标系 |
| `-J·ω_d^×·ω` | 补偿期望角速度耦合项 |
| `J·ω̇_d` | 前馈项：补偿期望角加速度 |
| `ω×Jω` | 欧拉力/科里奥利力补偿（刚体陀螺效应） |

---

## 15. 与标准 lpc2hpc 的关键差异对比

| 特性 | 标准 lpc2hpc（积分链） | so(3) 齐次控制 |
|------|----------------------|---------------|
| **系统模型** | `ẋ = Ax + Bu`，A 是常数矩阵 | `ξ̇ = [0 J_r⁻¹(θ_e); 0 0]ξ + Bu`，`J_r⁻¹` **依赖状态** |
| **Gd 的确定** | `Gd = I + μ·G₀`，G₀ 由块分解算出 | `Gd = [(1-μ)I₃, 0; 0, I₃]`，**人为设计** |
| **P 的确定** | Lyapunov 方程的解 | **参数化形式**，ε 通过不等式约束确定 |
| **K₀ 的角色** | 显式分解 `K₀ + (K-K₀)` | 不分解，直接用 **K = [-K₁, -k₂I₃]** |
| **状态空间** | ℝⁿ（无界） | `B³[π] × ℝ³`（**θ_e 有界**） |
| **全局性处理** | 无需特殊处理 | 需要**脉冲系统**（跳变机制） |
| **稳定性分析** | 标准 Lyapunov | 脉冲系统的齐次 Lyapunov 分析 |
| **证明关键恒等式** | 无需 | `θ_e' J_r⁻¹ ω_e = θ_e' ω_e`（等式 (27)） |

---

## 16. 扩展到四元数表示

### 16.1 四元数与指数坐标的对应

单位四元数 `q = [q₀; q_v] ∈ S³`（`q₀² + |q_v|² = 1`）与指数坐标 `θ ∈ B³[π]` 的关系：

```
q  = [cos(|θ|/2); sin(|θ|/2) · θ/|θ|]
θ  = 2 · arccos(|q₀|) · (q_v / |q_v|)    当 q_v ≠ 0
θ  = 0                                    当 q_v = 0 (即 q = ±[1; 0])
```

**关键**：从 q 可以无损地转换为 θ（ℝ³ 向量），因此两种表示在理论层面上等价。

### 16.2 四元数框架下的误差动力学

四元数误差：

```
q_e = q_d* ⊗ q    （⊗ = 四元数乘法）
    = [q_{e0}; q_{ev}]
```

指数坐标误差：

```
θ_e = 2 · arccos(|q_{e0}|) · (q_{ev} / |q_{ev}|) · φ
```

其中 `φ = sign(q_{e0})`（保证 θ_e 在 B³[π] 中）。

角速度误差 `ω_e` 的定义不变。

**误差动力学**：
```
ė_e = J_{r,q}⁻¹(θ_e) · ω_e
```
其中 `J_{r,q}⁻¹` 是四元数版本的右 Jacobian 逆。它与 so(3) 版本的 `J_r⁻¹` 通过 `θ_e` 一一对应。

### 16.3 跳变 → 四元数翻转

so(3) 脉冲系统的跳变：

```
当 |θ_e| = π 且 d/dt|θ_e| > 0 时:  θ_e → -θ_e
```

等价于四元数的翻转检测：

```
if q_{e0} < 0:          # 虚部 > 实部, 旋转角 > π
    q_e = -q_e           # 翻转四元数
    # 此时 θ_e 自动变号, 等价于跳变
```

**为什么等价**：

```
q_e = [cos(φ/2); sin(φ/2)·n]    (φ 是旋转角)
q_{e0} < 0 ⇔ cos(φ/2) < 0 ⇔ φ > π
```

但 `φ > π` 在指数坐标中超出 `B³[π]`。翻转 `q_e → -q_e`：

```
-q_e = [-cos(φ/2); -sin(φ/2)·n]
     = [cos((2π-φ)/2); sin((2π-φ)/2)·(-n)]
```

相当于用 `2π-φ < π` 的旋转角绕反方向轴 -n。这在指数坐标中就是 `θ_e → -θ_e`。

### 16.4 四元数版本的优势

| 方面 | so(3) 指数坐标 | 四元数 |
|------|--------------|--------|
| **跳变检测** | 需要检测 `\|θ_e\|` 是否达到 π | `q₀ < 0` 一个标量比较 |
| **跳变容差** | 需要处理浮点精度（`≈π`） | 不需要（翻转是精确操作） |
| **exp/log 计算** | 矩阵指数/对数（Rodrigues 公式） | 三角函数/反三角函数 |
| **工程兼容性** | 低 | **高**（PX4, ArduPilot 原生四元数） |
| **SE(3) 控制衔接** | 需矩阵运算 | 四元数已是主流 SE(3) 控制器标准 |
| **理论继承** | 已有证明（Zhou 2023） | 通过 θ_e 同构直接继承 |

### 16.5 四元数齐次控制器的伪代码

```
# === 误差计算 ===
q_e = q_d.conjugate() * q           # 四元数误差
if q_e.w < 0: q_e = -q_e            # 翻转（跳变）
θ_e = 2 * acos(q_e.w) * q_e.vec.normalized()

# === 状态向量 ===
ξ = [θ_e; ω_e]    # ω_e = R(q_e) * (ω - ω_d)

# === 齐次范数 ===
nx = hnorm(ξ, Gd, P)

# === 控制律 ===
u_hom = nx^(1+μ) * K * expm(-ln(nx) * Gd) * ξ

# === 力矩映射 ===
M = J * (R_d.T * u_hom - ω_d.cross(ω) + ω̇_d) + ω.cross(J * ω)
```

实际上，由于 so(3) 框架中 `u_hom` 的计算只需要 `θ_e` 和 `ω_e`，而两者都可以从四元数无损获得，**控制律形式完全不变**。

---

## 17. 总结

### 17.1 为什么"只能用于积分链"还能扩展到 SO(3)

不是齐次理论被扩展了，而是我们**换了一个视角看 SO(3)**：

```
SO(3) 的旋转矩阵  ──Log──→  so(3) 的指数坐标 θ_e ∈ B³[π] ⊂ ℝ³
                               ↓
                          ℝ³ 是向量空间
                          → 膨胀可以定义
                          → 齐次范数可以定义
                          → 齐次控制律可以直接写
                               ↓
                         唯一的额外代价：
                         在 |θ_e| = π 处需要脉冲跳变
                         (处理 SO(3) 的全局拓扑)
```

### 17.2 论文的贡献链条

```
1. Lie 代数 so(3) ≅ ℝ³ 提供向量空间结构
   ↓
2. 指数坐标 θ_e   将姿态误差表示为 ℝ³ 向量
   ↓
3. 脉冲系统        处理 |θ_e| = π 的歧义（全局性）
   ↓
4. Theorem 2       将齐次 Lyapunov 理论扩展到这类脉冲系统
   ↓
5. Theorem 3       具体设计姿态齐次控制器（加权膨胀）
   ↓
6. Corollary 1     通过切换实现固定时间精确收敛
   ↓
7. Corollary 2     ISS 鲁棒性分析（测量噪声/扰动）
```

### 17.3 与积分链 lpc2hpc 的根本区别

| 积分链 lpc2hpc | so(3) 齐次控制 |
|---|---|
| G₀ 由系统**块可控结构**决定 | Gd 由**设计者选择**（`(1-μ)I₃` 和 `I₃`） |
| 需要 K₀ 分解（`u = K₀x + ...`） | 不需要 K₀，直接用 `K = [-K₁  -k₂I₃]` |
| μ 的范围由 G₀ 和 P 的关系约束 | μ 的范围由 P 的正定性和 W ≺ 0 约束 |
| 无跳变（连续系统） | 需要跳变（脉冲系统） |
| P 由 Lyapunov 方程解出 | P 有参数化形式（ε 是自由参数） |

### 17.4 一句话总结

> 齐次控制从"只能用于积分链"到 SO(3) 姿态控制的扩展，本质上是利用 Lie 代数 so(3) 作为向量空间这一事实，在指数坐标上定义齐次控制器，并用脉冲系统处理 SO(3) 的全局拓扑障碍。对于工程实践，四元数是更友好的表示——翻转检测更简单、与现有飞控生态兼容。

---

## 参考文献

1. Zhou, Y., Polyakov, A., & Zheng, G. (2023). Generalized Homogeneous Rigid-Body Attitude Control. _IEEE Trans. Autom. Control_. arXiv:2210.01530v2.
2. Polyakov, A. (2020). _Generalized Homogeneity in Systems and Control_. Springer.
3. Bharadwaj, S., Osipchuk, M., Mease, K.D., & Park, F.C. (1998). Geometry and inverse optimality in global attitude stabilization. _JGCD_, 21(6):930-939.
4. Chirikjian, G.S. (2012). _Stochastic Models, Information Theory, and Lie Groups_, Volume 2. Springer.
5. Bullo, F. & Murray, R.M. (1995). Proportional derivative (PD) control on the Euclidean group. _ECC_.
6. Mayhew, C.G. & Teel, A.R. (2013). Synergistic hybrid feedback for global rigid-body attitude tracking on SO(3). _IEEE TAC_, 58(11):2730-2742.
7. Lee, T., Leok, M., & McClamroch, N.H. (2010). Geometric tracking control of a quadrotor UAV on SE(3). _CDC_.
