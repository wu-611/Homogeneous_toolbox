# 硕士课题详细执行计划

> 课题：SE(3) 几何齐次跟踪控制——从全状态反馈到输出反馈
> 周期：18 个月（3 学期 + 1 暑假）
> 产出目标：1 篇会议（CDC/ACC）+ 1 篇期刊（TAC/Automatica/TCST）

---

## 目录

- [Phase 0：基础夯实（月份 1-2）](#phase-0基础夯实月份-1-2)
- [Phase 1：全状态反馈 μ=0 版本（月份 3-6）](#phase-1全状态反馈-μ0-版本月份-3-6)
- [Phase 2：全状态反馈 μ<0 版本（月份 7-10）](#phase-2全状态反馈-μ0-版本月份-7-10)
- [决策点 A（月份 10 末）](#决策点-a月份-10-末)
- [Phase 3a：输出反馈扩展（月份 11-14，选做）](#phase-3a输出反馈扩展月份-11-14选做)
- [Phase 3b：SITL 实验 + 论文收尾（月份 11-14，必做）](#phase-3bsitl-实验--论文收尾月份-11-14必做)
- [Phase 4：论文撰写与答辩（月份 15-18）](#phase-4论文撰写与答辩月份-15-18)
- [附录 A：各 Phase 每周任务分解](#附录-a各-phase-每周任务分解)
- [附录 B：代码清单](#附录-b代码清单)
- [附录 C：风险登记表](#附录-c风险登记表)

---

## Phase 0：基础夯实（月份 1-2）

### 目标
建立完整的理论基础 + 代码工具链，确保后续工作不会因知识盲区或工具缺失而卡住。

### 任务列表

#### T0.1 文献精读（贯穿，重点在前 4 周）

| 文献 | 精读重点 | 产出 |
|------|---------|------|
| Polyakov (2020) 书，第 1-3 章 | 膨胀群、齐次范数、齐次系统稳定性理论 | 笔记 |
| Polyakov (2020) 书，第 11 章 | 齐次观测器、`lo2ho` 理论 | 笔记 |
| Zhou et al. (2023) | Theorem 2 和 Theorem 3 的证明细节 | 逐行推导 |
| Wang Siyuan (2020) 第 3 章 | `lpc2hpc` 在四旋翼上的应用、实验细节 | 笔记 |
| Wang Siyuan (2020) 第 4 章 | `lo2ho` 的工程应用、G₀ 构造、参数选择 | 笔记 |
| Lee et al. (2010) | SE(3) 几何控制器结构、Lyapunov 证明 | 笔记 |

**验收标准**：能独立写出 Zhou 2023 Theorem 3 的完整证明，不参考原文。

#### T0.2 代码审计与补全（2 周）

**T0.2.1 审计现有 Python 工具箱**（3 天）

逐文件检查 `hcs_toolbox_py/` 中每个函数的正确性：

```python
# 重点验证：
- hnorm.py    : 二分法迭代是否与 MATLAB 行为一致
- lpc2hpc.py  : 输出的 K0, G0, P, μ_min, μ_max 是否与 MATLAB 一致
- lpic2hpic.py: 同上
- e_hpc.py    : 控制量计算是否匹配 MATLAB 的 e_hpc.m
- e_hpic.py   : 同上
- ZOH.py      : 离散化结果是否匹配
```

方法：用相同的输入分别跑 MATLAB 和 Python，比较输出矩阵的逐元素差异（应 < 1e-10）。

**T0.2.2 移植缺失的观测器函数**（3 天）

```python
# hcs_toolbox_py/lo2ho.py
def lo2ho(A, C, L):
    """将 Luenberger 观测器升级为齐次观测器。
    对偶于 lpc2hpc: lo2ho(A,C,L) ≡ lpc2hpc(A',C',L') 的转置。"""
    K0, G0, P, nu_max, nu_min = lpc2hpc(A.T, C.T, L.T)
    return K0.T, -G0.T, P, -nu_min, -nu_max

# hcs_toolbox_py/e_ho.py
def e_ho(h, z, y, A, C, f, L0, L_nl, Gd, nu, alpha=1e-6, beta=np.inf):
    """齐次观测器显式 Euler 更新一步。"""
    nCe = np.linalg.norm(C @ z - y)
    nCe = max(alpha, min(beta, nCe))
    nCe = max(1e-20, nCe)
    n = A.shape[0]
    S = L0 + expm(np.log(nCe) * (nu * np.eye(n) + Gd - np.eye(n))) @ L_nl
    znew = (np.eye(n) + h * (A + S @ C)) @ z + h * f - h * S @ y
    return znew

# hcs_toolbox_py/si_ho.py  （半隐式离散化，对刚性系统更稳定）
# 参考 si_ho.m 实现
```

更新 `hcs_toolbox_py/__init__.py` 添加 `lo2ho`, `e_ho`, `si_ho`。

**T0.2.3 创建验证脚本**（2 天）

```python
# tests/validate_lo2ho.py
# 用 demo_lo2ho.m 的相同参数（倒立摆模型）验证 Python 输出与 MATLAB 一致

# tests/validate_e_ho.py
# 用 demo_ho.m 的相同参数（谐波振荡器）验证观测器轨迹一致
```

#### T0.3 搭建仿真框架（1 周）

**T0.3.1 仿真基类**

```python
# simulation/simulator.py
class Simulator:
    def __init__(self, dt, t_max):
        self.dt = dt
        self.t_max = t_max
        self.N = int(t_max / dt)

    def run(self, system, controller, x0):
        """通用仿真循环：支持全状态反馈和输出反馈"""
        pass
```

**T0.3.2 四旋翼 SE(3) 动力学模型**

```python
# models/quadrotor_se3.py
class QuadrotorSE3:
    """四旋翼 SE(3) 动力学
    状态：[x, y, z, vx, vy, vz, R(3×3), ωx, ωy, ωz]
    输入：[F_total, τx, τy, τz]
    """
    def __init__(self, m, J, g):
        pass
    def dynamics(self, state, u):
        """连续时间导数"""
        pass
    def step_rk4(self, state, u, dt):
        """RK4 积分"""
        pass
```

**T0.3.3 可视化工具**

```python
# visualization/plotter.py
# 3D 轨迹图、误差收敛图（对数坐标）、控制量图
# 对比模式：同时绘制多个控制器的结果
```

#### T0.4 复现关键仿真（1 周）

| 仿真 | 目的 | 验收 |
|------|------|------|
| `demo_lpc2hpc.py`（已有） | 确认工具箱正常 | 与 MATLAB 输出一致 |
| `demo_lo2ho.py`（新建） | 确认观测器移植正确 | 误差有限时间收敛到零 |
| `uav_homogeneous_control.py`（已有） | 确认三回路仿真正常 | 输出与已知结果一致 |
| Zhou 2023 姿态仿真复现 | 确认理解姿态齐次控制 | 收敛曲线与论文 Fig.1 趋势一致 |

### Phase 0 交付物

```
✅ 文献精读笔记（电子版，带页码引用）
✅ Python 工具箱完整（控制器 + 观测器，全部验证通过）
✅ 仿真框架（四旋翼模型 + 仿真循环 + 可视化）
✅ 关键仿真复现（至少 4 个）
```

---

## Phase 1：全状态反馈 μ=0 版本（月份 3-6）

### 目标
设计 SE(3) 几何齐次控制器（μ_p = μ_a = 0），完成级联稳定性证明，完成仿真对比，撰写会议论文。

### 任务列表

#### T1.1 位置回路 HPC 设计（2 周）

**输入**：四旋翼位置动力学（双积分器，3 通道解耦）

```
每个通道 i ∈ {x, y, z}：
  A_i = [0 1; 0 0],  B_i = [0; 1/m]
  线性增益：K_i（已有，来自 Wang 论文或 LQR 设计）
  → lpc2hpc(A_i, B_i, K_i) → K0_i, G0_i, P_i, μ_min_i, μ_max_i
  → 取 μ_i = 0,  Gd_i = I + 0·G0_i = I
```

**注意**：μ=0 时 `Gd = I`，`d(s) = expm(s·I) = e^s·I`，即均匀膨胀。此时齐次范数为普通的加权欧氏范数。这是最简单的非平凡情况。

**交付**：`design_position_hpc.py` —— 给定 m, K_i → 输出 K0_i, P_i, 验证函数。

#### T1.2 姿态回路 so(3) 齐次设计（2 周）

**输入**：四旋翼姿态动力学

参照 Zhou 2023 Theorem 3，但取 μ_a = 0：

```
ξ = [θ_e; ω_e]
K = [-K₁  -k₂I₃]    （线性 PD 增益）
Gd = [I₃   0 ]       （μ=0 时 G₀ 的贡献消失）
     [0    I₃]
P  = [I₃   εI₃]
     [εI₃  K₁⁻¹]
```

**交付**：`design_attitude_hpc.py` —— 给定 J, K₁, k₂ → 输出 Gd, P, ε。

#### T1.3 期望姿态 R_d 的解算（1 周）

SE(3) 几何控制器的关键环节：位置控制器输出期望推力方向 → 解算期望姿态。

```
F_des = m·(u_pos + g·e₃)    ← 位置 HPC 输出 + 重力补偿
b₃_des = F_des / |F_des|     ← 期望推力方向（Z 轴）
b₁_des = b₂_ref × b₃_des    ← 期望 X 轴（由偏航参考 b₂_ref 定义）
b₂_des = b₃_des × b₁_des    ← 期望 Y 轴
R_d = [b₁_des, b₂_des, b₃_des]
```

**交付**：`attitude_command.py` —— 已知 F_des, ψ_ref → 输出 R_d, ω_d, ω̇_d。

#### T1.4 力矩映射（1 周）

```
u_att = e_hpc(ξ, K₀_a, K_nl_a, Gd_a, μ_a=0, hn_a, α_a, β_a)
M = J·(R_d'·u_att - ω_d^×·ω + ω̇_d) + ω×Jω
```

**交付**：`torque_mapping.py` —— 已知 u_att, R_d, ω_d, ω̇_d, ω → 输出 M。

#### T1.5 级联 Lyapunov 分析（4 周，Phase 1 核心难点）

**第 1-2 周：建立数学模型**

定义联合 Lyapunov 函数：

```
V = V_p + V_a

V_p(e_p, e_v)  = ||[e_p; e_v]||_{d_p}    （位置齐次范数）
V_a(θ_e, ω_e)  = ||[θ_e; ω_e]||_{d_a}    （姿态齐次范数）
```

其中 `d_p` 是位置回路的膨胀（μ_p = 0 时退化为均匀膨胀），`d_a` 是姿态回路的膨胀（μ_a = 0 时退化为均匀膨胀）。

**第 3 周：推导 V_p 的导数中的耦合项**

位置误差动力学：

```
ė_p = e_v
ė_v = u_pos + g·(e₃ - R e₃ / |F_des| 的一部分)
```

耦合来自：期望推力方向在理想姿态 R_d 下为 `R_d e₃`，实际推力方向为 `R e₃`。

```
||R e₃ - R_d e₃|| = ||(R - R_d)e₃|| ≤ ||R - R_d|| ≤ c₁·||θ_e||
```

（最后的界利用 Zhou 2023 的结果——||R-R_d|| 与 ||θ_e|| 的关系在 B³(π) 上有界）

因此耦合项满足：

```
V̇_p ≤ -ρ_p·V_p + γ(V_a)
```

其中 γ(·) 是 K 类函数，表示姿态误差对位置收敛的扰动。

**第 4 周：证明 V̇ ≤ 0**

利用 `V̇_a ≤ -ρ_a·V_a`（Zhou 2023 Theorem 3 的结论，μ_a=0 时 ρ_a > 0）：

```
V̇ = V̇_p + V̇_a
   ≤ -ρ_p·V_p + γ(V_a) - ρ_a·V_a
   = -ρ_p·V_p - [ρ_a·V_a - γ(V_a)]
```

如果 `ρ_a·V_a ≥ γ(V_a)` 对所有 V_a 成立（通过选择足够大的 ρ_a / 足够的姿态增益），则 `V̇ ≤ -ρ_p·V_p < 0` 对所有 V > 0，证毕。

这是典型的**时间尺度分离**论证：姿态回路收敛速度 >> 位置回路。

**交付**：完整的 LaTeX 证明文档，包含所有不等式推导和条件。

#### T1.6 数值仿真（2 周）

**仿真场景**：

| 场景 | 初始位置误差 | 初始姿态误差 | 目的 |
|------|:-----------:|:-----------:|------|
| S1: 阶跃悬停 | e_xyz = [1, 0.5, 1]m | θ_e ≈ 0 | 验证位置跟踪 |
| S2: 螺旋上升 | e_xyz = [1, 1, 0]m | θ_e ≈ 30° | 验证动态跟踪 |
| S3: 大翻转恢复 | e_xyz ≈ 0 | θ_e ≈ 150° | 验证姿态全局性 + 跳变 |
| S4: 阵风扰动 | 悬停中 | 悬停中 | 验证鲁棒性 |

**对比控制器**：

1. Lee et al. (2010) SE(3) 几何 PD（标准 baseline）
2. Wang Siyuan (2020) 线性化 8D HPC（你的已有代码）
3. 本文方案（SE(3) 几何齐次，μ=0）

**量化指标**：

- 位置/姿态误差的 ISE（积分平方误差）
- 收敛时间（误差进入 1% 初始值的时间）
- 控制能量 E = (∫ |u(t)|² dt)^(1/2)
- 最大超调量

**交付**：完整的仿真脚本 + 对比图表（用于论文）。

#### T1.7 撰写会议论文（2 周）

**目标期刊/会议**：IEEE CDC 或 ACC

**论文结构**（6 页）：

```
1. Introduction (0.5 页)
   - 四旋翼控制 + 齐次控制背景
   - 贡献：首次将齐次控制与 SE(3) 几何控制统一

2. Preliminaries (1 页)
   - 齐次控制：膨胀、齐次范数、lpc2hpc
   - so(3) 齐次控制：Zhou 2023 关键结论

3. SE(3) Homogeneous Controller Design (1.5 页)
   - 位置回路 HPC 设计
   - 姿态回路 so(3) 设计
   - 级联架构

4. Stability Analysis (1.5 页)
   - 级联 Lyapunov 函数
   - 耦合界估计
   - 指数收敛证明

5. Simulation Results (1 页)
   - 3-4 个场景对比图表
   - 量化对比表

6. Conclusion (0.5 页)
```

### Phase 1 交付物

```
✅ 位置回路 HPC 设计模块（代码 + 验证）
✅ 姿态回路 so(3) 齐次设计模块（代码 + 验证）
✅ 期望姿态解算 + 力矩映射模块
✅ 级联稳定性证明（LaTeX 文档）
✅ 4 场景仿真对比（图表 + 数据）
✅ 会议论文初稿
```

---

## Phase 2：全状态反馈 μ<0 版本（月份 7-10）

### 目标
将 μ=0 推广到 μ<0，证明有限时间收敛，完成期刊论文主体。

### 任务列表

#### T2.1 齐次度推广（2 周）

**T2.1.1 位置回路 μ_p < 0**

从 μ_p = 0 切换到 μ_p = μ_min（`lpc2hpc` 给出的最负容许值）：

```
需要修改的：
- Gd_p = I + μ_p·G0_p    （不再是单位阵）
- hnorm_p(x, Gd_p, P_p)  （需二分法求解）
- 控制律中的 hn^(1+μ_p) 因子（非线性放大/衰减）
```

各通道的 μ_min 可能不同（由于各通道的质量/惯量参数相同，应该一致），但需检查。

**T2.1.2 姿态回路 μ_a < 0**

从 μ_a = 0 切换到 μ_a < 0：

```
Gd_a = [(1-μ_a)I₃     0   ]    （不再是单位阵）
       [   0          I₃  ]

P_a 中的 ε 需要重新计算（ε < min{ε_μ, ε̃} 中的 ε_μ 依赖 μ）
```

**验收**：μ=0 版本的所有仿真在 μ<0 下重新运行，确认收敛时间显著缩短。

#### T2.2 有限时间收敛分析（4 周，Phase 2 核心难点）

**第 1-2 周：单回路的有限时间分析**

由齐次理论（Proposition 2.1.1 / Zhou 2023 Theorem 3）：

```
V̇_p ≤ -ρ̃_p · V_p^(1+μ_p)    （μ_p < 0 时）
V̇_a ≤ -ρ̃_a · V_a^(1+μ_a)    （μ_a < 0 时）
```

收敛时间估计：

```
T_p ≤ V_p(0)^{-μ_p} / (-ρ̃_p · μ_p)
T_a ≤ V_a(0)^{-μ_a} / (-ρ̃_a · μ_a)
```

**第 3-4 周：级联系统的有限时间分析**

关键问题：位置回路的耦合项在有限时间框架下如何界定？

```
V̇_p ≤ -ρ̃_p · V_p^(1+μ_p) + γ(V_a)
```

耦合项 `γ(V_a)` 依赖于姿态误差。如果姿态回路有限时间收敛：

```
V_a(t) = 0,  ∀t ≥ T_a
```

则在 `t ≥ T_a` 之后，位置回路的扰动项消失：

```
V̇_p ≤ -ρ̃_p · V_p^(1+μ_p),  ∀t ≥ T_a
```

总收敛时间：

```
T_total(x₀) ≤ T_a(V_a(0)) + T_p(V_p(T_a))
```

**简化情况**：如果 `μ_a ≤ μ_p < 0`（姿态比位置更快收敛），`T_a ≪ T_p`，总收敛时间近似等于位置回路的独立收敛时间。

**交付**：完整的有限时间收敛证明，含收敛时间上界表达式。

#### T2.3 仿真对比（2 周）

在 Phase 1 的相同场景上对比：

| 控制器 | μ |
|--------|---|
| Lee 几何 PD | — |
| 本文 μ=0 | μ_p=0, μ_a=0 |
| **本文 μ<0** | **μ_p<0, μ_a<0** |

重点展示：
- μ<0 版本在有限时间内精确收敛（对数坐标可看到误差降到 1e-30 以下）
- μ=0 版本呈指数收敛（误差曲线在对数坐标上为直线）
- Lee 几何 PD 作为参考线

#### T2.4 期刊论文扩展（2 周）

将会议论文（6 页）扩展为期刊论文（14-16 页）：

```
新增内容：
- 完整的预备知识（独立一节，含齐次观测器简介作为 background）
- 有限时间收敛的完整证明
- 收敛时间估计与调参讨论
- μ_p 与 μ_a 的选择策略
- 更丰富的仿真（增加参数敏感性分析）
```

### Phase 2 交付物

```
✅ μ<0 版本控制器（代码）
✅ 有限时间收敛证明（LaTeX）
✅ 收敛时间估计公式
✅ 增强版仿真对比（含对数坐标的有限时间收敛展示）
✅ 期刊论文初稿（14-16 页）
```

---

## 决策点 A（月份 10 末）

**根据 Phase 2 的进度和剩余时间，选择路径**：

| 条件 | 选择 | 理由 |
|------|------|------|
| Phase 2 按期完成，剩余 4+ 月 | **Phase 3a + 3b**（输出反馈 + SITL + 论文） | 最大化论文质量 |
| Phase 2 基本完成，有限时间分析还有差距 | **Phase 3b only**（SITL + 论文，输出反馈仅仿真验证） | 保证按时毕业 |
| Phase 2 严重延期 | **砍掉输出反馈**，集中精力完成期刊论文 + 毕业 | 最低可行方案 |

---

## Phase 3a：输出反馈扩展（月份 11-14，选做）

### 目标
将位置回路从全状态反馈替换为齐次观测器 + 齐次控制器的输出反馈方案。

### 任务列表

#### T3a.1 位置回路输出反馈设计（2 周）

**每个通道独立设计**：

```python
# 系统模型：双积分器，只测位置
A = np.array([[0, 1], [0, 0]])
C = np.array([[1, 0]])

# 线性观测器增益（参考 Wang 2020 第 4 章）
# 通过极点配置或 LQG 设计
L_lin = ...   # 2×1 矩阵

# 升级为齐次观测器
L0, G0, P_obs, nu_min, nu_max = lo2ho(A, C, L_lin)
nu = nu_min  # 取最负值 → 最快有限时间估计
Gd_obs = np.eye(2) + nu * G0
L_nl = L_lin - L0

# 验证：观测误差 ê = [e_pos; e_vel] 应有限时间收敛到 0
```

**选择 ν 的策略**：
- `|ν| > |μ_p|`：观测器比控制器收敛更快（推荐，有利于复合稳定）
- `ν < 0`：观测误差有限时间收敛

**G₀ 的构造**（参考 Wang 2020 第 4 章对位置通道的处理）：

```
G₀ 需满足：
  1. A·G₀ = (G₀ + I)·A    （A 的 d-齐次性条件）
  2. C·G₀ = 0              （输出在膨胀下不变）

对双积分器 [0 1; 0 0]，一个可能的选择：
  G₀ = [0 0; 0 1]    ← 满足上述两个条件
```

**交付**：`design_position_ho.py`

#### T3a.2 观测器离散化（1 周）

实际飞控以离散时间运行（PX4 通常是 100-250Hz）。需确认离散化方法：

```python
# 显式 Euler（简单，对慢系统足够）
z = e_ho(dt, z, y_pos, A, C, B*u_ctrl, L0, L_nl, Gd_obs, nu, alpha, beta)

# 或 ZOH 离散化（更精确）
# 对观测器动态方程做 ZOH 离散化
```

**验收**：连续时间 + 离散时间仿真对比，确认离散化不引入显著误差。

#### T3a.3 复合系统分析（3 周，难点）

**方案 A（如果时间/能力允许）**：尝试证明复合系统的 ISS 稳定性

联合 Lyapunov 函数：

```
V_total = V_obs(e) + V_ctrl(x̂)
        = ||e||_{d_obs} + ||x̂||_{d_ctrl}
```

其中 `e = z - x`（观测误差），`x̂ = z`（估计状态 = 控制器输入）。

闭合系统方程：

```
ė   = (A + L₀C + g_obs(Ce))·e     ← 观测误差（独立于控制器）
x̂̇  = A·x̂ + B·u(x̂) + g_obs(Ce)   ← 估计状态（受观测误差驱动）
u   = K₀·x̂ + g_ctrl(x̂)            ← 控制器使用估计状态
```

**关键挑战**：真实状态 `x` 和估计状态 `x̂` 的动力学通过 `u(x̂)` 耦合，不能简单分离。

**够用的结果**（如果严格证明太难）：
- 证明在 `|ν| ≫ |μ_p|` 条件下（观测器远快于控制器），复合系统局部 ISS 稳定
- 或证明存在一个吸引域，使得从该区域内出发的轨迹收敛

**方案 B**：不试图严格证明，而是：
- 给出 ISS 的充分条件（基于时间尺度分离）
- 通过仿真（含不同 ν/μ 组合的 Monte Carlo 测试）验证复合系统的经验稳定域
- 在论文中明确标注"严格的分离原理证明留待未来工作"

**交付**：分析文档（LaTeX）或充分的仿真验证数据。

#### T3a.4 输出反馈仿真（2 周）

在 Phase 2 的仿真场景上，对比全状态反馈 vs. 输出反馈：

| 方案 | 位置回路 | 姿态回路 |
|------|---------|---------|
| 全状态反馈 | 需要 v_x, v_y, v_z | 全状态（IMU） |
| 输出反馈 | **仅需 x, y, z（位置）** | 全状态（IMU） |

**增加场景**：
- 速度初始估计误差大（z₀ 的初始速度猜测远离真实值，如猜测为 0 但实际为 5 m/s）
- 位置测量噪声（加高斯白噪声，模拟 GPS 误差）
- 观测器参数 a, b, ν 的敏感性

### Phase 3a 交付物

```
✅ 位置回路 HO 设计模块
✅ 复合系统分析（严格证明或充分条件 + 仿真验证）
✅ 输出反馈 vs. 全状态反馈对比仿真
✅ 噪声/不确定性下的鲁棒性验证
```

---

## Phase 3b：SITL 实验 + 论文收尾（月份 11-14，必做）

### 目标
基于已有 ROS2 框架实现 SITL 验证，完成论文全部章节。

### 任务列表

#### T3b.1 SITL 环境搭建（1 周）

```
- 确认 PX4-Autopilot SITL 可用
- 确认 Micro XRCE-DDS Agent 可用
- 确认 ROS2 px4_msgs 可用
- 验证基本通信：ros2 topic list | grep fmu
```

#### T3b.2 控制器 ROS2 节点实现（2 周）

参考你已有的开发规范文档，实现：

```cpp
// hcs_uav_controller.cpp（基于你已有的框架）

// 1. 订阅 PX4 状态
vehicle_odometry_sub_ = ...
// 提取：位置(NED)、速度(NED)、姿态(四元数)、角速度(FRD)

// 2. 坐标转换
NED_to_ZUp(...)    // 位置、速度
FRD_to_ZUp(...)    // 角速度
quat_to_R(...)     // 四元数 → 旋转矩阵

// 3. 误差计算
e_pos = pos - target_pos
e_vel = vel - target_vel   // 如果使用输出反馈，此项来自观测器
theta_e = Log(R * R_d')    // 姿态指数坐标误差
omega_e = R_d * (omega - omega_d)

// 4. 齐次控制器
if (use_output_feedback):
    z_pos = e_ho(dt, z_pos, e_pos, A_pos, C_pos, B*u_pos, ...)
    e_vel_est = z_pos[1]
    u_pos = e_hpc([e_pos; e_vel_est], K0_p, K_nl_p, Gd_p, mu_p, ...)
else:
    u_pos = e_hpc([e_pos; e_vel], K0_p, K_nl_p, Gd_p, mu_p, ...)

u_att = e_hpc([theta_e; omega_e], K0_a, K_nl_a, Gd_a, mu_a, ...)

// 5. 力矩映射
M = J * (R_d' * u_att - omega_d.cross(omega) + omega_d_dot) + omega.cross(J * omega)

// 6. 坐标逆转换 + 发布
F_zup = [0; 0; m * u_pos_z + m * g]  // Z-Up 推力
F_ned = [F_zup(0); F_zup(1); -F_zup(2)]  // → NED
publish_vehicle_thrust_setpoint(F_ned)
publish_vehicle_torque_setpoint(M)
```

#### T3b.3 SITL 测试（2 周）

| 测试 | 命令 | 验证标准 |
|------|------|---------|
| 悬停 | target_z=-2.0 | 稳态位置误差 < 0.2m |
| X 阶跃 | target_x=1.0 | 无超调，收敛 < 3s |
| Y 阶跃 | target_y=1.0 | 同上 |
| Z 阶跃 | target_z=-3.0 | 同上 |
| 偏航阶跃 | target_yaw=1.57 | 平滑旋转 90° |
| 8 字轨迹 | 时变 target | 跟踪误差 < 0.3m RMS |
| 阵风扰动 | Gazebo wind plugin | 恢复时间 < 2s |

#### T3b.4 结果整理与论文完成（3 周）

- 仿真结果 + SITL 结果整合
- 完成所有章节
- 制作答辩 PPT 初稿

### Phase 3b 交付物

```
✅ ROS2 控制器节点（含输出反馈选项）
✅ SITL 测试报告（所有场景的数据 + 视频截图）
✅ 论文完整初稿
✅ 答辩 PPT 初稿
```

---

## Phase 4：论文撰写与答辩（月份 15-18）

### 任务列表

#### T4.1 期刊论文润色与投稿（4 周）

- 导师审阅 + 修改
- 语言润色（建议找英语母语者或专业服务）
- 格式检查（目标期刊模板）
- 提交

#### T4.2 硕士论文撰写（6 周）

按章节计划（见创新方向文档第三章）逐一完成。

#### T4.3 答辩准备（2 周）

- PPT 终稿
- 预答辩
- 可能的修改

### Phase 4 交付物

```
✅ 期刊论文投稿
✅ 硕士论文终稿
✅ 答辩通过
```

---

## 附录 A：各 Phase 每周任务分解

### Phase 0（月份 1-2，共 8 周）

| 周 | 任务 |
|----|------|
| 1 | Polyakov 2020 第 1-3 章精读 |
| 2 | Zhou 2023 精读 + 证明推导 |
| 3 | Wang 2020 第 3-4 章精读 + Lee 2010 SE(3) 几何控制 |
| 4 | 完成文献笔记 + 确认理解所有关键证明 |
| 5 | 审计 hcs_toolbox_py 现有函数 + 移植 lo2ho/e_ho/si_ho |
| 6 | 编写验证脚本 + 搭建仿真框架（模型 + 仿真循环） |
| 7 | 复现关键仿真（4 个）+ 可视化 |
| 8 | 补充缺失内容 + Phase 0 收尾 |

### Phase 1（月份 3-6，共 16 周）

| 周 | 任务 |
|----|------|
| 1-2 | T1.1 位置回路 HPC 设计 |
| 3-4 | T1.2 姿态回路 so(3) 齐次设计 |
| 5 | T1.3 期望姿态解算 |
| 6 | T1.4 力矩映射 |
| 7-8 | T1.5 级联 Lyapunov 分析（数学模型 + 导数推导） |
| 9-10 | T1.5 继续（耦合界 + V̇ ≤ 0 证明） |
| 11-12 | T1.6 数值仿真（4 场景 × 3 控制器） |
| 13-14 | T1.7 撰写会议论文 |
| 15 | 论文修改 + 导师反馈 |
| 16 | 投稿 + Phase 1 收尾 |

### Phase 2（月份 7-10，共 16 周）

| 周 | 任务 |
|----|------|
| 1-2 | T2.1 齐次度推广（μ<0） |
| 3-4 | T2.2 有限时间分析（单回路） |
| 5-6 | T2.2 继续（级联合并 + T_total 公式） |
| 7-8 | T2.3 增强版仿真 |
| 9-10 | T2.4 期刊论文扩展（引言+预备知识+正文） |
| 11-12 | T2.4 继续（证明+仿真+结论） |
| 13 | 期刊论文修改 |
| 14 | **决策点 A**：评估进度，选择 Phase 3a/3b 策略 |
| 15-16 | 缓冲 / 补充分析 |

### Phase 3a/3b（月份 11-14，共 16 周）

| 周 | 3a（输出反馈） | 3b（SITL + 收尾） |
|----|---------------|-------------------|
| 1-2 | T3a.1 位置回路 HO 设计 | T3b.1 环境搭建 |
| 3 | T3a.2 观测器离散化 | T3b.2 ROS2 控制器 |
| 4-6 | T3a.3 复合系统分析 | T3b.2 ROS2 控制器（续） |
| 7-8 | T3a.4 输出反馈仿真 | T3b.3 SITL 测试 |
| 9-10 | 结果整理 | T3b.3 SITL 测试（续） |
| 11-12 | 论文章节完善 | T3b.4 结果整理 |
| 13-14 | 第二篇论文初稿（如适用） | 论文初稿修改 |
| 15-16 | 缓冲 | 缓冲 |

### Phase 4（月份 15-18，共 16 周）

| 周 | 任务 |
|----|------|
| 1-4 | 期刊论文润色 + 投稿 |
| 5-10 | 硕士论文撰写 |
| 11-12 | 导师审阅 + 修改 |
| 13-14 | 答辩准备 |
| 15-16 | 答辩 |

---

## 附录 B：代码清单

### B.1 需要新建的文件

```
hcs_toolbox_py/
├── lo2ho.py              # 齐次观测器升级（对偶 lpc2hpc）
├── e_ho.py               # 齐次观测器显式 Euler 更新
├── si_ho.py              # 齐次观测器半隐式更新
└── tests/
    ├── validate_lo2ho.py # lo2ho 验证（对比 MATLAB）
    └── validate_e_ho.py  # e_ho 验证

models/
├── quadrotor_se3.py      # 四旋翼 SE(3) 全动力学
└── quadrotor_linear.py   # 四旋翼线性化模型（已有参考）

design/
├── design_position_hpc.py    # 位置回路 HPC 参数设计
├── design_attitude_hpc.py    # 姿态回路 so(3) 参数设计
├── design_position_ho.py     # 位置回路 HO 参数设计（Phase 3a）
├── attitude_command.py       # 期望姿态解算 R_d, ω_d, ω̇_d
└── torque_mapping.py         # u_att → 实际力矩 M

simulation/
├── simulator.py              # 通用仿真循环
├── scenario_step.py          # 阶跃响应场景
├── scenario_spiral.py        # 螺旋轨迹场景
├── scenario_flip.py          # 大翻转恢复场景
└── scenario_wind.py          # 阵风扰动场景

controllers/
├── se3_homogeneous_full.py   # 全状态反馈 SE(3) 齐次控制器
└── se3_homogeneous_outfb.py  # 输出反馈 SE(3) 齐次控制器（Phase 3a）

visualization/
├── plotter.py                # 通用绘图（3D 轨迹、误差、控制量）
└── comparison_table.py       # 量化对比表生成

scripts/
├── run_phase1_simulations.py # Phase 1 所有仿真一键运行
├── run_phase2_simulations.py # Phase 2 所有仿真一键运行
└── run_comparison.py         # 最终对比（所有控制器 vs. 所有场景）
```

### B.2 需要修改的现有文件

```
hcs_toolbox_py/__init__.py    # 添加 lo2ho, e_ho, si_ho 的 import
```

---

## 附录 C：风险登记表

| 编号 | 风险 | 概率 | 影响 | 应对 |
|:----:|------|:----:|:----:|------|
| R1 | 级联 Lyapunov 分析中耦合项无法严格界定 | 中 | 高 | 退而求其次：ISS 型证明（仅需界存在，不需显式表达式）；或采用"姿态指数收敛+位置输入-状态稳定"的组合论证 |
| R2 | μ<0 时齐次范数的计算成本过高（二分法每步 20 次 expm） | 中 | 中 | 使用 ANN 近似（HCS toolbox 已有 `hnorm_ANN.m`）；或将齐次范数的计算频率降低（如每 5 步更新一次） |
| R3 | so(3) 脉冲跳变在实际连续仿真中反复触发（Zeno 行为） | 低 | 高 | 严格按 Zhou 2023 的条件：`D = {|θ_e|=π, θ_e'ω_e>0}`，跳变后 `ΠD` 不在 D 中；仿真中加 hysteresis |
| R4 | 姿态回路 ε 的可行区间太窄（`ε < min{ε_μ, ε̃}`），参数选择困难 | 低 | 中 | 用 LMI 求解器（cvxopt）数值求解 ε 的可行范围；如 LMI 无解，调整 K₁, k₂ |
| R5 | 复合系统分析（HO+HPC）做不出严格证明 | 高 | 中 | 方案 B：仅给出 ISS 充分条件 + 充分的仿真经验验证；Wang 2020 也没有做这个证明，审稿人不会苛求 |
| R6 | SITL 中 PX4 版本不兼容或 DDS 通信异常 | 中 | 中 | 提前固定 PX4 版本（v1.14.3）；准备纯仿真备选方案（仅数值仿真，不加 SITL） |
| R7 | 论文被拒（会议或期刊） | 中 | 中 | 准备备选投稿目标（CDC 备选 ECC/ACC；TAC 备选 Automatica/TCST）；根据审稿意见修改后重投 |
| R8 | 硕士论文进度落后 | 中 | 高 | 决策点 A（月份 10）是硬性检查点——如果落后超过 4 周，立即砍掉 Phase 3a，集中完成最低可行版本 |
